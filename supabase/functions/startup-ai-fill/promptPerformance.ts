// [AI 작성하기] 실적 밴드 6종 프롬프트 — 연혁 · 트랙션·고객 · 매출·재무 · 고용 · 주주 · 투자.
//
// 실적은 **기간마다 다시 재는 값**이다. 그래서 이 여섯의 지시는 "언제의 값인가"를 가장 먼저
// 묻고, 시점을 특정할 수 없는 값은 행을 만들지 않는다(월을 지어내면 표의 가로축이 거짓이
// 되고, 그 표는 추이를 그리려고 만든 것이라 한 칸의 거짓이 선 전체를 망친다).
//
// 확정 숫자(재무제표)와 자기 보고(기업 제시)를 가르는 것은 자리가 아니라 카드다 —
// revenue 는 결산만 받고 traction 은 문서 표현을 그대로 옮긴다. 두 카드가 나란히 서는 이유는
// 심사역이 "MAU는 느는데 매출이 안 따라오는가"를 견주기 때문이다.
//
// 근거: docs/docs_planning/3_3_5_startup_ai_fill.md §7.5~§7.10

import { CUSTOMER_KIND_OPTIONS, LIMITS } from './cards.ts'

const opts = (list: readonly string[]) => list.join(' / ')

export const TIMELINE_PROMPT = `[연혁] 회사의 사건을 시간순 목록으로 정리합니다. 문서에 '연혁·History·주요 이력' 섹션이 있으면 그것을
우선 옮기고, 없으면 문서 전반에서 날짜가 붙은 사건(설립·법인 전환·투자 유치·제품 출시·수상·인증·특허 등록·
주요 계약)을 모읍니다.

- 각 원소 { date, content }
  · date: YYYY-MM. 일까지 있으면 YYYY-MM-DD. 연도만 있으면 그 사건은 넣지 않고 notes에 남깁니다
    (월을 지어내지 않습니다).
  · content: 40자 이내 명사구. 예: "시리즈A 30억 유치(KB인베스트먼트)" / "QIS 2.0 정식 출시"
- 정렬: 오래된 것부터. 최대 ${LIMITS.timeline}건. 넘으면 투자·출시·계약 순으로 중요한 것을 남기고
  notes에 "N건 생략"을 적습니다.
- 같은 사건이 두 문서에 있으면 한 번만 적습니다.

없을 때: 날짜 있는 사건이 하나도 없으면 [].`

export const TRACTION_PROMPT = `[트랙션·고객] 기업이 스스로 보고한 지표와 레퍼런스입니다. 이 카드의 값은 화면에 '기업 제시'로 표기되므로
검증된 숫자처럼 다듬지 않고 문서의 표현을 그대로 옮깁니다.

- traction (핵심 지표): 각 원소
  · metric: 지표명 원문(MAU · 유료 고객 수 · GMV · 재구매율 · 파이프라인 금액 등). 문서가 쓴 이름을 그대로.
  · unit: 명 · 건 · % · 원 · 개 등. 금액이면 "원"이고 value는 원 단위 정수.
  · period: 기준월 YYYY-MM. 월이 특정되지 않으면(연도만·"현재") 행을 만들지 않고 notes에
    "MAU 12000(시점 미상)"으로 남깁니다.
  · value: 숫자. "약 1만" → 10000.
  같은 지표의 여러 시점은 각각 한 행입니다(추이를 그리기 위한 값입니다). 최대 ${LIMITS.traction}행.
- customers (주요 고객·레퍼런스): 각 원소
  · name: 고객사·기관명 원문.
  · kind (고정 선택지 — ${opts(CUSTOMER_KIND_OPTIONS)}): "납품·공급 계약·수주·구매" → 계약 /
    "업무협약·MOU·파트너십 체결" → MOU / "시범 사업·파일럿·실증·PoC·테스트 도입" → POC. 특정 불가면 null.
  · date: YYYY-MM. 없으면 null.
  로고만 나열되고 관계가 적히지 않았으면 name만 적고 kind는 null. 최대 ${LIMITS.customers}행.

없을 때: 두 목록 모두 비면 카드를 null로 둡니다.`

export const REVENUE_PROMPT = `[매출·재무] 연도 결산 기준 확정 실적만 적습니다. 화면에 '재무제표 기준'으로 표기되는 카드이므로 규칙이
가장 엄격합니다.

공통:
- 모든 금액은 원 단위 정수. "12.5억" → 1250000000, "350백만" → 350000000, "(단위: 천원) 1,234" → 1234000.
  표 머리의 단위를 반드시 확인합니다. 단위를 확인할 수 없는 표는 옮기지 않고 notes에 남깁니다.
- 손실·부채 초과는 음수(-). 괄호 표기 (123)은 음수입니다.
- 전망·추정·목표(E, F, 예상, 목표, 계획)는 제외합니다. "2026(E)" 열은 옮기지 않습니다.
- 분기·반기·누적("3분기 누적") 값은 연도 실적이 아니므로 제외하고 notes에 남깁니다.
- 재무제표·감사보고서·결산서 값이 IR 자료의 값과 다르면 재무제표를 택하고 notes에 두 값을 함께 남깁니다.

- revenue (손익): 각 원소 { year, revenue(매출액), operatingProfit(영업이익), netIncome(당기순이익) }.
  연도마다 한 행. 있는 값만 채우고 없는 값은 null.
- finance (재무상태): 각 원소 { year, assets(자산총계), liabilities(부채총계), equity(자본총계) }.
  자본총계가 음수면 그대로 음수로 적습니다(자본잠식). 연도마다 한 행.

없을 때: 결산 숫자가 없으면 카드를 null로 둡니다. "매출 발생 전"·"프리레브뉴" 표현이 있으면 notes에 그 사실을
남깁니다.`

export const EMPLOYEE_PROMPT = `[고용] 연도별 임직원 수입니다.

- 각 원소 { year, employeeCount }
  · year: 기준 연도. "2025.06 기준 12명"은 year 2025.
  · employeeCount: 정수. 정규직·계약직 구분이 있으면 합계를 적고 notes에 구분 값을 남깁니다.
    대표 포함 여부가 명시되면 notes에 남깁니다.
  · 같은 연도에 여러 시점이 있으면 가장 늦은 시점 값 하나만 적습니다.
  · 채용 목표·계획 인원은 제외합니다(팀·조직 카드의 hiringPlan이 받습니다).
- 조직도의 사람 수를 세어 만들지 않습니다. 명시된 인원수만 적습니다.

없을 때: [].`

export const SHAREHOLDERS_PROMPT = `[주주] 특정 시점의 주주 구성 스냅샷입니다. 주주명부·캡테이블·지분 구조 표가 문서에 있을 때만 작성합니다.
본문 문장 속 "대표 지분 60%"처럼 표 없이 한 값만 있으면 스냅샷을 만들지 않고 notes에 남깁니다.

- 각 원소 { date, holders[] }
  · date: 기준일 YYYY-MM-DD. 일이 없고 월만 있으면 빈 문자열("")로 두고 notes에 "기준월 YYYY-MM"을 남깁니다.
    날짜를 지어내지 않습니다.
  · holders: 각 원소 { name(주주명 원문), shares(보유 주식 수, 정수, 없으면 null),
    percentage(지분율 숫자, % 기호 없음, 없으면 null) }.
    "기타 소액주주 5인 3%"처럼 묶인 줄은 name "기타(5인)"으로 한 행.
- 표가 여러 시점(라운드별)이면 각각 스냅샷 하나입니다. 오래된 것부터.
- 검증: 한 스냅샷의 percentage 합이 99~101 밖이면 값은 그대로 적고 notes에 "지분율 합계 N% — 확인 필요"를
  남깁니다. 합을 맞추기 위해 값을 고치지 않습니다.

없을 때: [].`

export const INVESTMENT_PROMPT = `[투자] 완료된 투자 유치 라운드입니다. 진행 중·예정·목표 라운드("시리즈A 유치 예정",
"현재 라운드 목표 50억")는 제외하고 notes에 남깁니다.

- 각 원소 { date, round, fundingAmount, valuation, investor }
  · date: 납입·클로징 시점 YYYY-MM. 월이 없으면 행을 만들지 않고 notes에 남깁니다.
  · round: 표기 통일 — 시드/씨드 → "Seed", 프리A/프리시리즈A → "Pre-A", 시리즈A/B/C → "Series A"/"Series B"/
    "Series C", 브릿지 → "Bridge", 엔젤 → "Angel", 정부 매칭·TIPS 연계 투자 → "TIPS". 특정 불가면 null.
  · fundingAmount: 유치 금액, 원 단위 정수. 통화가 USD 등이면 원문 금액·통화를 notes에 남기고 이 칸은 null로
    둡니다(환율을 적용하지 않습니다).
  · valuation: 기업가치, 원 단위 정수. 프리/포스트머니가 명시되면 포스트머니를 적고 notes에 구분을 남깁니다.
    구분이 없으면 값은 적고 notes에 "프리/포스트 미상"을 남깁니다. 없으면 null.
  · investor: 투자자명을 " · "로 연결. 리드 투자자가 명시되면 맨 앞에 두고 "(리드)"를 병기합니다.
- 오래된 것부터. 최대 ${LIMITS.investment}행.
- 누적 투자액만 있고 라운드별 값이 없으면 행을 만들지 않고 notes에 "누적 투자 N원(라운드 분해 불가)"를 남깁니다.

없을 때: [].`
