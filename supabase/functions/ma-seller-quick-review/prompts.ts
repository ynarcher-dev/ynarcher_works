// [M&A 셀러 퀵 리뷰] 공통 규약 + 절별 지시 조립 — **1단계(사실 추출)의 프롬프트**.
//
// 절별 지시 앞에 늘 붙는 것이 여기 있다. 공통으로 올린 기준은 하나 — **절이 늘어도 답이
// 달라지면 안 되는 규칙**이다. 근거 없는 값을 쓰지 않는 것, 표기 규격, 상충 처리는 절마다
// 다시 정할 일이 아니고, 절마다 적으면 일곱 벌이 조금씩 어긋난다.
//
// ## 이 파일이 쓰는 것은 '사실'이고 '문장'이 아니다(2026-09-08 개정)
//
// 종전에는 이 프롬프트 하나가 사실 수집과 문장 작성을 함께 맡았고, 그래서 표기 규격이
// **명사구 강제**였다("서술어를 쓰지 않습니다"). 사실을 빠짐없이 담게 하려면 그 규격이 맞다 —
// 문장으로 쓰라고 하면 모델은 매끄러움을 위해 값을 버린다. 그런데 그 규격에서 나온 결과는
// 사람이 쓴 퀵 리뷰가 아니라 **데이터 시트**였다. 값은 정확한데 읽을 수 없었다.
//
// 그래서 일을 둘로 갈랐다. 여기는 **빠짐없이 담는 일**만 하고, 문장으로 세우는 일은 2단계
// 작문 패스(`compose.ts`)가 같은 사실을 입력으로 받아 한다. 가른 근거는 두 일이 서로를
// 방해한다는 것이다 — 한 요청에서 함께 시키면 매끄러운 문장이 사실을 밀어내거나(값 누락),
// 빠짐없이 담느라 문장이 끊긴다(명사구 나열). 두 번 물으면 각자 자기 일만 한다.
//
// **그래서 여기의 문체 규격은 그대로 명사구다.** 2단계가 다시 쓰지 않는 칸(표·주주·단서·
// 제품별 실적)은 이 규격이 최종본이고, 원래 그 칸들은 문장이 아니라 값이 서는 자리다.
//
// STARTUP 쪽 프롬프트와 문장을 공유하지 않는다. 둘이 지금은 비슷하지만 **묻는 것이 다르기**
// 때문이다 — 저쪽은 원장의 칸을 채우고 이쪽은 읽는 사람을 앞에 둔 문서를 쓴다. 한 벌로 묶으면
// 한쪽의 문체를 고칠 때마다 다른 쪽의 결과가 함께 흔들린다.
//
// 근거: docs/docs_planning/3_6_1_ma_seller_quick_review.md §5.4

import { CARD_KEYS, CARD_LABELS, LIMITS, type CardKey } from './cards.ts'
import type { LedgerFacts } from './ledger.ts'

/**
 * 역할과 원칙 — 근거 없는 값을 만들지 않는다는 이 기능의 계약.
 *
 * 3번이 '수식어 금지'에서 '수식어를 근거로 바꿔 적기'로 바뀐 것이 2026-09-08의 변경이다.
 * 종전 규칙은 뒷받침할 수치가 없으면 **그 줄 자체를 버리라**고 했는데, 그래서 사실은 맞지만
 * 문서에 있던 정성적 강점(직공급 구조·독점 계약·규제 변화)이 통째로 사라졌다. 파는 쪽 자료를
 * 옮겨 적어 홍보문이 되는 것을 막는 방법은 줄을 지우는 것이 아니라 **형용사를 사실로 바꾸는
 * 것**이다 — '압도적 1위'는 버리고 '점유율 61.4%'를 남긴다.
 */
const ROLE_RULES = `당신은 M&A 자문사의 딜 스크리닝 자료 작성 보조자입니다. 첨부된 문서(IM·티저·사업계획서·재무제표·
IR 자료 등)만을 근거로, 매각 대상 기업의 '퀵 리뷰' 문서에 들어갈 **사실**을 아래 JSON 스키마에 맞춰
빠짐없이 옮깁니다.

절대 규칙:
1. 문서에 없는 사실은 쓰지 않습니다. 문서 밖 지식(업계 상식·다른 기업 사례·시장 통계)을 끌어오지 않고,
   문서에 없는 숫자를 만들지 않습니다. 다만 문서의 사실에서 **곧바로 따라오는 서술**(중간 유통 단계를
   생략한다 → 유통 마진을 남긴다)은 그 사실과 한 줄에 붙여 적을 수 있습니다.
2. 근거를 찾지 못한 항목은 null(목록형은 [])로 둡니다. 빈 값은 실패가 아니라 정답입니다.
3. 마케팅 수식어(혁신적·세계 최초·독보적·압도적·최고의)는 **문서에 그 표현이 있어도** 그대로 옮기지 않고,
   그것을 뒷받침하는 수치·사실로 **바꿔** 적습니다("압도적 1위" → "점유율 61.4%로 1위").
   뒷받침할 사실이 문서에 없으면 수식어만 지우고 나머지는 남깁니다 — 줄 자체를 버리지 않습니다.
   이 문서는 파는 쪽이 만든 자료를 근거로 하므로, 옮겨 적기만 하면 홍보문이 됩니다.
4. 여러 문서가 같은 항목에 다른 값을 말하면: 감사보고서·재무제표·등기 자료 > IM·티저 > IR·소개 자료 순으로
   우선하고, 같은 등급이면 작성일이 늦은 문서를 택한 뒤 notes에 상충 사실을 남깁니다.
5. 전망·목표·예정·계획 값(E, 예상, 목표, ~할 것)은 실적 항목에 넣지 않습니다. Valuation 절만 예외이며,
   그 절에서도 **가정이라는 사실을 문장 안에 남깁니다**(예: "27년 정상화 가정 시").
6. 감사 여부·기준 시점 같은 단서(未감사, 딜 전 기준, 잠정)는 지우지 않고 해당 절의 note 칸에 옮깁니다.
   그 한 줄이 빠지면 잠정 숫자가 확정 숫자로 읽힙니다.
7. 출력은 지정된 JSON 하나만 냅니다. 코드펜스·설명 문장을 덧붙이지 않습니다.`

/**
 * 표기 규격 — 명사구 원칙. **이 단계에서는 문장이 아니라 값이 목적이다.**
 *
 * 숫자 표기(FY·전년比·괄호 증감률)를 여기 모아 둔 것이 2026-09-08의 추가다. 종전에는 규격이
 * 단위와 부호까지만 정해서, 같은 연도가 `2025년`·`FY25`·`'25`로 절마다 다르게 적혔다. 표기가
 * 절마다 갈리면 2단계가 그것을 그대로 물려받아 한 문서 안에서 같은 해가 세 이름으로 선다.
 */
const FORMAT_RULES = `서술 규격(모든 문자열 항목 공통):
- 명사구로 끝냅니다. 서술어("~합니다", "~있음", "~하고 있다")를 쓰지 않습니다.
  **이 단계의 목적은 값을 빠짐없이 담는 것이고, 문장으로 다듬는 일은 뒤에서 따로 합니다.**
  그러니 매끄러움을 위해 값을 줄이지 말고, 아는 것을 전부 적으십시오.
- 한 항목 안의 여러 요소는 가운뎃점(" · ")으로 잇습니다.
- 사실과 그 함의를 한 줄에 붙일 때는 화살표(" → ")를 씁니다("중간 유통 단계 생략 → 마진 방어").
- 고유명사·영문 약어·제품명·브랜드명은 문서 원문 표기를 유지합니다(번역·풀어쓰기 금지).
- 문자열 안의 숫자는 세 자리마다 쉼표를 넣습니다(예: 50,000개소 · 3,500만 캔).
- JSON 숫자 필드는 쉼표 없는 숫자로 씁니다.
- **금액 숫자 필드의 단위는 백만원입니다**(1억 = 100, 557억 = 55700). 문서의 표가 백만원 단위면 그 값을
  그대로, 억 단위면 100을 곱해 옮깁니다. 통화 기호를 넣지 않습니다.
- **문자열 안에서 금액을 말할 때는 억 단위**로 적습니다("매출 557억"). 표는 백만원, 문장은 억이며
  두 자리가 서로 다른 단위를 쓰는 것은 표가 자릿수를 견주는 자리이고 문장은 크기를 말하는 자리이기 때문입니다.
- 손실·마이너스 값은 음수로 씁니다(문서의 괄호 표기 (2,924)는 -2924).
- 연도(fiscalYear)는 네 자리 서기 연도입니다(FY25A → 2025).
- **문자열 안에서 회계연도를 부를 때는 FY 표기**를 씁니다(FY25A · FY22A~FY25A). 실적이면 A, 추정이면 E를
  문서가 붙인 그대로 유지합니다.
- 증감률은 괄호와 부호로 적습니다("매출 557억(+31.2%)"). 전년 대비임을 밝힐 때는 "전년比"를 씁니다.
- 비율은 % 기호 없이 숫자만(61.4) — 단, 문자열 안에서는 % 기호를 붙입니다.
- 날짜는 YYYY-MM. 일 단위가 필요하면 YYYY-MM-DD. 연도만 아는 값은 그 연도를 문자열 칸에 그대로 적습니다.`

/**
 * 절 간 역할 분담 — **같은 사실을 두 절에 적지 않는다.**
 *
 * 2026-09-08에 추가했다. 실제 산출물에서 `summary.headline`과 `basics.businessDescription`이
 * **글자까지 같은 문장**으로 나왔다. 두 절이 한 요청(overview 묶음)에서 함께 채워지니 모델이
 * 먼저 쓴 문장을 그대로 복사한 것이다. 절마다 무엇을 담는지는 각 절 지시에 적혀 있었지만,
 * **무엇을 담지 않는지**가 없으면 경계가 겹치는 자리에서 같은 값이 두 번 선다.
 *
 * 화면에서는 두 줄이 위아래로 붙어 서므로 같은 문장이 두 번 읽힌다 — 문서가 스스로를 반복하는
 * 것으로 보이고, 그 순간 나머지 절의 값도 의심받는다.
 */
const ROLE_SPLIT = `절 간 역할 분담(같은 문장을 두 절에 적지 않습니다):
- summary(한줄 요약)는 **판단**입니다 — 이 회사를 한 문장으로 말하면 무엇인가. 문서의 첫 줄입니다.
- basics.businessDescription(사업내용)은 **정체성**입니다 — 무엇을 만들어 어디에 파는가.
  실적·점유율·매출을 여기 적지 않습니다(그 값은 요약과 재무 절이 답합니다).
- intro.body(소개 본문)는 **구조**입니다 — 어떤 자산과 경로로 그 사업이 돌아가는가.
- intro.bullets(추가 사실)는 본문이 담지 못한 **개별 사실**입니다. 본문의 문장을 다시 적지 않습니다.
- highlights(핵심 포인트)는 **왜 이 건인가**입니다. 앞 절의 숫자를 근거로 다시 인용해도 되지만,
  앞 절과 어긋난 숫자를 쓰지 않습니다.
같은 사실이 두 절에 필요하면 **각자 다른 각도로** 적습니다. 문장을 복사하지 않습니다.`

/**
 * 봉투 — notes는 저장되지 않고 검토에만 쓴다.
 *
 * **근거(evidence) 규칙은 여기 없다.** 근거는 우리가 발급한 조각 id로 답해야 하고 그 id를
 * 발급하는 것도 대조하는 것도 엔진이라, 지시가 이쪽에 있으면 규약이 두 곳에 살게 된다 —
 * 어긋나는 날 모든 근거가 조용히 버려진다(_shared/aiFill/evidence.ts).
 */
const ENVELOPE_RULES = `출력 봉투:
- cards: 아래 지시가 있는 절만 키로 갖습니다. 지시하지 않은 절은 넣지 않습니다.
- notes: 절별 문자열 목록. 문서에 있으나 규격 칸에 넣지 못한 값, 상충·확인 필요 경고를 남깁니다.
- notes는 절마다 최대 ${LIMITS.notes}줄, 한 줄 80자 이내입니다.
- 절 전체에 근거가 없으면 객체형 절은 null, 목록형 절은 []로 둡니다.`

const CARD_PROMPTS: Record<CardKey, string> = {
  summary: `[summary] 한줄 요약
- headline: 이 기업이 무엇으로 어느 위치에 있고 최근 실적이 어떤지를 **한 문장**으로 압축합니다.
  들어갈 요소는 셋이고 이 순서입니다 — ① 무엇을 하는 회사인가(사업·카테고리) · ② 시장에서의
  위치(점유율·순위·유통망 규모 등 수치) · ③ 최근 실적(매출과 전년 대비 증감률).
  셋 중 문서에 없는 것은 빼고 씁니다(지어내지 않습니다). 120자 이내.
- 이 절은 문서의 첫 줄이라 뒤 절의 요약이 아니라 **읽는 사람이 계속 읽을지 정하는 줄**입니다.
  형용사 대신 숫자를 넣습니다.
- **사업내용(basics)과 같은 문장을 쓰지 않습니다.** 저쪽은 무엇을 파는 회사인지만 말하고,
  여기는 그것이 시장에서 어떤 위치이고 실적이 어떤지까지 말합니다.`,

  basics: `[basics] 주요내용
- companyName: 법인 정식 명칭. 영문 상호가 함께 적혀 있으면 괄호로 병기합니다.
- foundedOn: 설립 시점. 연도만 알면 "2017년"처럼 문서 표기를 그대로 씁니다.
- headquarters: 본사 소재지. 공장·지점이 함께 적혀 있으면 가운뎃점으로 잇습니다.
- representative: 대표자 성명.
- businessDescription: 사업 내용 — **무엇을 만들어 어느 채널로 파는가**. 명사구 1~2줄, 160자 이내,
  줄바꿈 최대 1회. **실적·점유율·매출·순위를 적지 않습니다**(요약과 재무 절의 몫입니다).
- shareholders: 주주 목록. name(주주명)과 ratio(지분율 숫자)로 나눠 담고, 지분율 큰 순으로 세웁니다.
  '기타 주주' 같은 묶음 행도 그대로 한 줄로 담습니다(합계가 100이 되어야 표가 사실을 말합니다).
- shareholdersAsOf: 주주구성의 기준 시점("딜 전, '26.5월 말 기준" 등). 문서에 있으면 반드시 옮깁니다 —
  이 값이 없으면 지분율이 시점 없는 숫자가 됩니다.
- note: 이 절의 단서(잠정·未감사·기준일 등). 없으면 null.
- **요약재무(자산·부채·자본·매출)를 여기 적지 않습니다.** 재무 절의 가장 최근 연도가 그 값이며,
  두 곳에 적으면 한쪽만 고쳐 어긋납니다.`,

  intro: `[intro] 회사 소개
- metrics: 문서 상단에 타일로 서는 핵심 지표 **최대 ${LIMITS.metrics}개**. label(지표 이름)과 value(값)로 나눕니다.
  예: label "국내 RTD 점유율" / value "61.4%", label "CVS 유통망" / value "50,000+ 개소".
  값에는 단위를 포함하고, 라벨에는 넣지 않습니다. 문서가 타일로 세우지 않았어도 본문에서 가장 많이
  인용되는 수치 넷을 고릅니다.
- body: 회사 소개 본문 — **이 사업이 어떤 자산과 경로로 돌아가는가**. 담을 축은 셋입니다:
  ① 무엇을 파는가(제품·카테고리) · ② 어떻게 파는가(유통 구조·고객) · ③ 무엇으로 버티는가
  (기술·생산능력·IP·계약). 300자 이내.
- bullets: 소개 본문이 담지 못한 사실을 줄로 세웁니다. 최대 ${LIMITS.introBullets}줄, 한 줄 120자 이내.
  **줄마다 수치나 확인 가능한 사실이 하나는 들어가야 합니다.** 수치가 없으면 그 줄이 무엇을 근거로
  하는지 말하지 못합니다. 본문에 이미 쓴 문장을 다시 적지 않습니다.`,

  products: `[products] 제품·서비스
- body: 제품 라인업을 한 줄로 훑는 서술. 브랜드명을 문서 표기 그대로 나열합니다. 240자 이내.
- items: 제품별 실적 표. product(제품명)와 achievement(그 제품의 실적)로 나눕니다.
  achievement에는 **판매량·순위·기간처럼 검증 가능한 사실만** 담습니다("인기 제품" 같은 평가는 담지 않습니다).
  최대 ${LIMITS.products}줄.
- bullets: 제품·기술에 관한 추가 사실(개발 기간·보유 레시피 수·공법 등). 최대 ${LIMITS.productBullets}줄.
- note: 표의 단서. 없으면 null.`,

  financials: `[financials] 재무 요약
- pnl: 손익 표. 회계연도마다 한 줄이고 오래된 연도부터 세웁니다. 최대 ${LIMITS.fiscalYears}년.
  칸은 fiscalYear · netRevenue(순매출) · grossProfit(매출총이익) · ebitda · ebit · adjustedEbitda(조정 EBITDA).
- bs: 재무상태표. 같은 규칙으로 세웁니다. 칸은 fiscalYear · cash(현금 및 현금성자산) ·
  interestBearingDebt(이자부채) · unpaidTax(미지급세금) · totalAssets(자산총계) ·
  totalLiabilities(부채총계) · totalEquity(자본총계).
- **성장률·이익률·Net debt를 적지 않습니다.** 위 값으로 계산되는 값이라 화면이 직접 계산합니다.
  스키마에 그 칸이 없는 것이 그 이유이며, 문서의 % 행은 옮기지 않고 지나갑니다.
- 단위 확인이 이 절에서 가장 자주 틀립니다. 표 머리의 단위 표기(백만원·억원·천원)를 먼저 읽고
  **백만원으로 환산해** 담습니다. 단위를 확정하지 못하면 그 표를 비우고 notes에 사유를 남깁니다 —
  잘못된 단위의 숫자는 빈 칸보다 나쁩니다.
- note: 감사 여부·조정 근거 같은 단서를 그대로 옮깁니다(예: "25년 재무자료는 未감사 기준",
  "조정 EBITDA는 일회성 비용 조정"). 이 절에서 이 줄은 빠뜨리면 안 되는 값입니다.`,

  valuation: `[valuation] Valuation
- bullets: **이 회사의 기업가치를 얼마로 보는가**의 논리만 담습니다. 최대 ${LIMITS.valuationBullets}줄, 한 줄 140자 이내.
- **가정과 결과를 한 줄 안에 함께 적습니다**(예: "'27년 정상화 가정 · 영업이익률 20% 적용 시 EBITDA 120억",
  "EV/EBITDA 8배 적용 시 EV 960억"). 결과만 적으면 확정된 값으로 읽힙니다.
- **여기 담지 않는 것**: 라운드별 조달 목표(Pre-A에 회원 30만·매출 10억 등), 희망 투자금액·투자
  조건·발행 주식 종류·목표 지분율. 그것들은 기업의 가치가 아니라 **딜의 조건**이고, 조달 계획은
  달성 여부를 아직 아무도 모르는 목표라 가치 산정의 근거가 되지 못합니다.
  문서에 그런 값이 있으면 notes에 한 줄로 옮기고 bullets에는 넣지 않습니다.
- 문서에 밸류에이션 언급이 없으면 [] 로 둡니다. 배수·시장 관행을 끌어와 계산하지 않습니다.`,

  highlights: `[highlights] 핵심 포인트
- 묶음 최대 ${LIMITS.highlights}개. 각 묶음은 title(포인트 제목)과 bullets(근거 줄)로 이뤄집니다.
- title: **주장 — 부제** 꼴로 적습니다. 앞은 그 포인트를 한 마디로 요약한 명사구(12자 이내),
  뒤는 그것이 왜 값어치가 있는지를 덧붙이는 짧은 구입니다. 뒤가 없으면 앞만 적습니다.
  예: "압도적 시장 지배력" · "생산 인프라 완비 — 추가 Capex 불필요" · "규제 테일윈드 — 주세법 개정 수혜".
- bullets: 그 제목을 뒷받침하는 사실. 묶음마다 최대 ${LIMITS.highlightBullets}줄, 한 줄 140자 이내.
  **줄마다 수치나 확인 가능한 사실이 하나는 있어야 합니다.**
  사실만 적고 끝내지 말고, 그 사실이 무엇을 뜻하는지를 화살표로 이어 붙입니다
  ("CVS 직공급 구조 → 중간 유통 단계 생략 → 마진 방어 및 실시간 판매 데이터 확보").
- 앞 절에 이미 담긴 숫자를 다시 인용해도 됩니다 — 이 절은 새 사실이 아니라 **왜 사야 하는가**를
  묶는 자리입니다. 다만 앞 절과 숫자가 어긋나서는 안 됩니다.
- 포인트는 문서가 세운 순서를 따릅니다. 문서가 순서를 정하지 않았으면 시장 지위 → 사업 구조 →
  생산·공급 → 규제·외부 환경 → 확장성 순으로 세웁니다.`,
}

/**
 * 체크된 절의 지시만 이어 붙인다. 고르지 않은 절의 지시를 함께 보내지 않는 이유는 비용이
 * 아니라 결과다 — 지시가 있으면 모델은 채우려 하고, 화면이 쓰지 않을 값을 만드느라 정작
 * 고른 절의 근거 탐색이 얕아진다.
 */
export function buildPrompt(cards: CardKey[], subject: string, ledger: LedgerFacts | null): string {
  // 절 순서는 요청 순서가 아니라 문서 순서로 고정한다. 요청 순서를 그대로 쓰면 같은 조합인데
  // 담당자가 체크한 차례에 따라 프롬프트가 달라진다.
  const ordered = CARD_KEYS.filter((k) => cards.includes(k))
  const sections = ordered.map((k) => CARD_PROMPTS[k]).join('\n\n---\n\n')
  const names = ordered.map((k) => `${k}(${CARD_LABELS[k]})`).join(', ')

  return [
    ROLE_RULES,
    FORMAT_RULES,
    // 절이 둘 이상일 때만 붙인다 — 한 절만 채우는 요청에서는 겹칠 상대가 없다.
    ordered.length > 1 ? ROLE_SPLIT : null,
    ENVELOPE_RULES,
    // 확정 사실은 **절별 지시 앞**에 선다 — 무엇을 이미 아는지를 읽은 뒤라야 절마다 무엇을
    // 더 찾아야 하는지가 정해진다. 연결이 없거나 원장이 비면 이 자리가 통째로 빠진다.
    ledger ? renderLedgerFacts(ledger) : null,
    `매각 대상 기업: ${subject || '(문서에서 확인)'}\n채울 절: ${names}`,
    '--- 절별 지시 ---',
    sections,
  ]
    .filter((s): s is string => s !== null)
    .join('\n\n')
}

// ── 이미 확인된 사실(연결한 스타트업 원장) ────────────────────────────────
//
// **이 블록이 있는 요청과 없는 요청은 하는 일이 다르다.** 없으면 모델은 자료에서 전부를 뽑고,
// 있으면 **확정된 것 위에 자료가 더하는 것만** 찾는다. 그래서 담당자는 같은 자료를 두 번 읽히지
// 않고, 같은 기업이 두 화면에서 다른 매출로 서지 않는다.
//
// 규칙을 절별 지시가 아니라 블록 바로 옆에 두는 것이 요점이다 — 이 값을 어떻게 다룰지는 절마다
// 다시 정할 일이 아니고, 절마다 적으면 일곱 벌이 조금씩 어긋난다(ROLE_RULES와 같은 판단).

/**
 * 확정 사실을 다루는 규칙 넷.
 *
 * 2번이 이 기능에서 되돌릴 수 없는 사고가 나는 자리다 — 원장 값이 언제나 옳지는 않고(새 감사
 * 보고서가 갱신한다) 모델이 조용히 한쪽을 고르면 어느 쪽이 맞는지 아무도 모른다. 그래서
 * **자료를 우선하되 차이를 반드시 남기게** 한다. 고치는 것은 사람의 일이다.
 *
 * 3번은 근거 규약과의 충돌을 막는다 — 엔진은 우리가 발급한 조각 id로만 근거를 인정하므로
 * (evidence.ts) 원장에서 온 값에 근거를 달면 그 근거는 대조에 실패해 버려지고, 버려진 건수가
 * 카드 경고로 서서 멀쩡한 초안이 의심받는다.
 */
const LEDGER_RULES = `아래 '이미 확인된 사실'은 이 기업의 담당자가 **자료를 보고 확정해 저장한 값**입니다.
1. 이 값은 첨부 문서와 **동등한 근거**입니다. 문서에 없어도 여기 있으면 그대로 씁니다(지어낸 값이 아닙니다).
2. 같은 항목을 문서가 다르게 말하면 **문서 값을 쓰고**, 그 절의 notes에 "확인된 사실과 다름:
   원장 <값> / 문서 <값>" 한 줄을 남깁니다. 조용히 한쪽을 고르지 않습니다 — 어느 쪽이 맞는지는
   사람이 정합니다.
3. 여기서 가져온 값에는 **근거(evidence)를 달지 않습니다.** 그 값의 출처는 문서가 아니라 원장입니다.
4. 여기 없는 항목을 이 블록에서 유추하지 않습니다. 없는 것은 문서에서 찾고, 문서에도 없으면 비웁니다.`

/** 금액 한 칸. 비면 칸을 세우지 않는다 — 빈 칸이 늘어서면 표가 무엇을 말하는지 흐려진다. */
function money(label: string, v: number | null): string | null {
  return v === null ? null : `${label} ${v.toLocaleString('en-US')}`
}

/** 확정 사실을 프롬프트에 실을 글로 세운다. 값이 하나도 없으면 null(빈 표제를 세우지 않는다). */
export function renderLedgerFacts(f: LedgerFacts): string | null {
  const lines: string[] = []

  // 평면 칸들 — 한 줄에 모은다. 칸마다 줄을 주면 빈 칸이 그 수만큼 줄을 먹는다.
  const head = [
    f.name ? `회사명 ${f.name}` : null,
    f.companyForm ? `형태 ${f.companyForm}` : null,
    f.representative ? `대표자 ${f.representative}` : null,
    f.foundedOn ? `설립 ${f.foundedOn}` : null,
    f.location ? `소재지 ${f.location}` : null,
  ].filter((s): s is string => s !== null)
  if (head.length > 0) lines.push(head.join(' · '))

  for (const d of f.descriptions) lines.push(`${d.label}: ${d.value}`)

  // 표는 연도마다 한 줄이고 **단위를 머리에 한 번 적는다** — 줄마다 적으면 그 글자가 값보다
  // 길어지고, 안 적으면 모델이 원 단위로 읽어 백만 배가 된다.
  if (f.revenue.length > 0) {
    lines.push('손익(백만원):')
    for (const r of f.revenue) {
      const cols = [money('매출', r.revenue), money('영업이익', r.operatingProfit), money('당기순이익', r.netIncome)]
        .filter((s): s is string => s !== null)
        .join(' · ')
      if (cols) lines.push(`  ${r.year} — ${cols}`)
    }
  }
  if (f.finance.length > 0) {
    lines.push('재무상태(백만원):')
    for (const r of f.finance) {
      const cols = [money('자산', r.assets), money('부채', r.liabilities), money('자본', r.equity)]
        .filter((s): s is string => s !== null)
        .join(' · ')
      if (cols) lines.push(`  ${r.year} — ${cols}`)
    }
  }

  if (f.shareholders.holders.length > 0) {
    // 기준 시점을 제목에 붙인다 — 이 값이 없으면 지분율이 시점 없는 숫자가 된다(basics 지시와 같은 이유).
    const asOf = f.shareholders.asOf ? `(기준 ${f.shareholders.asOf})` : ''
    lines.push(`주주${asOf}: ${f.shareholders.holders
      .map((h) => (h.ratio === null ? h.name : `${h.name} ${h.ratio}%`))
      .join(' · ')}`)
  }

  if (lines.length === 0) return null
  return [LEDGER_RULES, '--- 이미 확인된 사실(스타트업 원장) ---', ...lines].join('\n')
}
