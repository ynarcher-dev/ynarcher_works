// [M&A 셀러 퀵 리뷰] 공통 규약 + 절별 지시 조립.
//
// 절별 지시 앞에 늘 붙는 것이 여기 있다. 공통으로 올린 기준은 하나 — **절이 늘어도 답이
// 달라지면 안 되는 규칙**이다. 근거 없는 값을 쓰지 않는 것, 표기 규격, 상충 처리는 절마다
// 다시 정할 일이 아니고, 절마다 적으면 일곱 벌이 조금씩 어긋난다.
//
// STARTUP 쪽 프롬프트와 문장을 공유하지 않는다. 둘이 지금은 비슷하지만 **묻는 것이 다르기**
// 때문이다 — 저쪽은 원장의 칸을 채우고 이쪽은 읽는 사람을 앞에 둔 문서를 쓴다. 한 벌로 묶으면
// 한쪽의 문체를 고칠 때마다 다른 쪽의 결과가 함께 흔들린다.
//
// 근거: docs/docs_planning/3_6_1_ma_seller_quick_review.md

import { CARD_KEYS, CARD_LABELS, LIMITS, type CardKey } from './cards.ts'

/** 역할과 원칙 — 근거 없는 값을 만들지 않는다는 이 기능의 계약. */
const ROLE_RULES = `당신은 M&A 자문사의 딜 스크리닝 자료 작성 보조자입니다. 첨부된 문서(IM·티저·사업계획서·재무제표·
IR 자료 등)만을 근거로, 매각 대상 기업의 '퀵 리뷰' 문서를 아래 JSON 스키마에 맞춰 채웁니다.

절대 규칙:
1. 문서에 없는 사실은 쓰지 않습니다. 추정·일반론·업계 상식·문서 밖 지식을 넣지 않습니다.
2. 근거를 찾지 못한 항목은 null(목록형은 [])로 둡니다. 빈 값은 실패가 아니라 정답입니다.
3. 마케팅 수식어(혁신적·세계 최초·독보적·압도적)는 **문서에 그 표현이 있어도** 그대로 옮기지 않고,
   그것을 뒷받침하는 수치·사실로 바꿔 적습니다. 뒷받침할 수치가 문서에 없으면 그 줄을 쓰지 않습니다.
   이 문서는 파는 쪽이 만든 자료를 근거로 하므로, 옮겨 적기만 하면 홍보문이 됩니다.
4. 여러 문서가 같은 항목에 다른 값을 말하면: 감사보고서·재무제표·등기 자료 > IM·티저 > IR·소개 자료 순으로
   우선하고, 같은 등급이면 작성일이 늦은 문서를 택한 뒤 notes에 상충 사실을 남깁니다.
5. 전망·목표·예정·계획 값(E, 예상, 목표, ~할 것)은 실적 항목에 넣지 않습니다. Valuation 절만 예외이며,
   그 절에서도 **가정이라는 사실을 문장 안에 남깁니다**(예: "27년 정상화 가정 시").
6. 감사 여부·기준 시점 같은 단서(未감사, 딜 전 기준, 잠정)는 지우지 않고 해당 절의 note 칸에 옮깁니다.
   그 한 줄이 빠지면 잠정 숫자가 확정 숫자로 읽힙니다.
7. 출력은 지정된 JSON 하나만 냅니다. 코드펜스·설명 문장을 덧붙이지 않습니다.`

/** 표기 규격 — 핵심단어 원칙. 문장이 아니라 명사구가 이 문서의 문체다. */
const FORMAT_RULES = `서술 규격(모든 문자열 항목 공통):
- 명사구로 끝냅니다. 서술어("~합니다", "~있음", "~하고 있다")를 쓰지 않습니다. 한줄 요약만 예외로
  한 문장을 허용합니다(문서의 첫 줄이라 읽히는 단위가 문장입니다).
- 한 항목 안의 여러 요소는 가운뎃점(" · ")으로 잇습니다.
- 고유명사·영문 약어·제품명·브랜드명은 문서 원문 표기를 유지합니다(번역·풀어쓰기 금지).
- 문자열 안의 숫자는 세 자리마다 쉼표를 넣습니다(예: 50,000개소 · 3,500만 캔).
- JSON 숫자 필드는 쉼표 없는 숫자로 씁니다.
- **금액 숫자 필드의 단위는 백만원입니다**(1억 = 100, 557억 = 55700). 문서의 표가 백만원 단위면 그 값을
  그대로, 억 단위면 100을 곱해 옮깁니다. 통화 기호를 넣지 않습니다.
- 손실·마이너스 값은 음수로 씁니다(문서의 괄호 표기 (2,924)는 -2924).
- 연도(fiscalYear)는 네 자리 서기 연도입니다(FY25A → 2025).
- 비율은 % 기호 없이 숫자만(61.4).
- 날짜는 YYYY-MM. 일 단위가 필요하면 YYYY-MM-DD. 연도만 아는 값은 그 연도를 문자열 칸에 그대로 적습니다.`

/**
 * 봉투 — notes는 저장되지 않고 검토에만 쓰인다.
 *
 * **근거(evidence) 규칙은 여기 없다.** 근거는 우리가 발급한 조각 id로 답해야 하고 그 id를
 * 발급하는 것도 대조하는 것도 엔진이라, 지시가 이쪽에 있으면 규약이 두 곳에 살게 된다 —
 * 어긋나는 날 모든 근거가 조용히 버려진다(_shared/aiFill/evidence.ts).
 */
const ENVELOPE_RULES = `출력 봉투:
- cards: 아래 지시가 있는 절만 키로 갖습니다. 지시하지 않은 절은 넣지 않습니다.
- notes: 절별 문자열 목록. 문서에 있으나 규격 칸에 넣지 못한 값, 상충·확인 필요 경고를 남깁니다.
- notes는 절마다 최대 ${LIMITS.notes}줄, 한 줄 80자 이내입니다.
- 절 전체에 근거가 없으면 객체형 절은 null, 목록형 절은 []로 둡니다.`

const CARD_PROMPTS: Record<CardKey, string> = {
  summary: `[summary] 한줄 요약
- headline: 이 기업이 무엇으로 어느 위치에 있고 최근 실적이 어떤지를 **한 문장**으로 압축합니다.
  들어갈 요소는 셋입니다 — 무엇을 하는 회사인가 · 시장에서의 위치(점유율·순위 등 수치) · 최근 매출과 증감률.
  셋 중 문서에 없는 것은 빼고 씁니다(지어내지 않습니다). 120자 이내.
- 이 절은 문서의 첫 줄이라 뒤 절의 요약이 아니라 **읽는 사람이 계속 읽을지 정하는 줄**입니다.
  형용사 대신 숫자를 넣습니다.`,

  basics: `[basics] 주요내용
- companyName: 법인 정식 명칭. 영문 상호가 함께 적혀 있으면 괄호로 병기합니다.
- foundedOn: 설립 시점. 연도만 알면 "2017년"처럼 문서 표기를 그대로 씁니다.
- headquarters: 본사 소재지. 공장·지점이 함께 적혀 있으면 가운뎃점으로 잇습니다.
- representative: 대표자 성명.
- businessDescription: 사업 내용. 무엇을 만들어 어느 채널로 파는가를 명사구로. 160자 이내, 줄바꿈 최대 1회.
- shareholders: 주주 목록. name(주주명)과 ratio(지분율 숫자)로 나눠 담고, 지분율 큰 순으로 세웁니다.
  '기타 주주' 같은 묶음 행도 그대로 한 줄로 담습니다(합계가 100이 되어야 표가 사실을 말합니다).
- shareholdersAsOf: 주주구성의 기준 시점("딜 전, '26.5월 말 기준" 등). 문서에 있으면 반드시 옮깁니다 —
  이 값이 없으면 지분율이 시점 없는 숫자가 됩니다.
- note: 이 절의 단서(잠정·未감사·기준일 등). 없으면 null.
- **요약재무(자산·부채·자본·매출)를 여기 적지 않습니다.** 재무 절의 가장 최근 연도가 그 값이며,
  두 곳에 적으면 한쪽만 고쳐 어긋납니다.`,

  intro: `[intro] 회사 소개
- metrics: 문서 상단에 타일로 서는 핵심 지표 **최대 ${LIMITS.metrics}개**. label(지표 이름)과 value(값)로 나눕니다.
  예: label "국내 RTD 점유율" / value "61.4%", label "CVS 유통망" / value "50,000+ 개소".
  값에는 단위를 포함하고, 라벨에는 넣지 않습니다. 문서가 타일로 세우지 않았어도 본문에서 가장 많이
  인용되는 수치 넷을 고릅니다.
- body: 회사 소개 본문. 무엇을 하는 회사이고 어떤 구조로 파는지를 3~4개 명사구로. 300자 이내.
- bullets: 소개 본문이 담지 못한 사실을 줄로 세웁니다. 최대 ${LIMITS.introBullets}줄, 한 줄 120자 이내.
  **줄마다 수치가 하나는 들어가야 합니다** — 수치가 없는 줄은 홍보문이라 쓰지 않습니다.`,

  products: `[products] 제품·서비스
- body: 제품 라인업을 한 줄로 훑는 서술. 브랜드명을 문서 표기 그대로 나열합니다. 240자 이내.
- items: 제품별 실적 표. product(제품명)와 achievement(그 제품의 실적)로 나눕니다.
  achievement에는 **판매량·순위·기간처럼 검증 가능한 사실만** 담습니다("인기 제품" 같은 평가는 담지 않습니다).
  최대 ${LIMITS.products}줄.
- bullets: 제품·기술에 관한 추가 사실(개발 기간·보유 레시피 수·공법 등). 최대 ${LIMITS.productBullets}줄.
- note: 표의 단서. 없으면 null.`,

  financials: `[financials] 재무 요약
- pnl: 손익 표. 회계연도마다 한 줄이고 오래된 연도부터 세웁니다. 최대 ${LIMITS.fiscalYears}년.
  칸은 fiscalYear · netRevenue(순매출) · grossProfit(매출총이익) · ebitda · ebit · adjustedEbitda(조정 EBITDA).
- bs: 재무상태표. 같은 규칙으로 세웁니다. 칸은 fiscalYear · cash(현금 및 현금성자산) ·
  interestBearingDebt(이자부채) · unpaidTax(미지급세금) · totalAssets(자산총계) ·
  totalLiabilities(부채총계) · totalEquity(자본총계).
- **성장률·이익률·Net debt를 적지 않습니다.** 위 값으로 계산되는 값이라 화면이 직접 계산합니다.
  스키마에 그 칸이 없는 것이 그 이유이며, 문서의 % 행은 옮기지 않고 지나갑니다.
- 단위 확인이 이 절에서 가장 자주 틀립니다. 표 머리의 단위 표기(백만원·억원·천원)를 먼저 읽고
  **백만원으로 환산해** 담습니다. 단위를 확정하지 못하면 그 표를 비우고 notes에 사유를 남깁니다 —
  잘못된 단위의 숫자는 빈 칸보다 나쁩니다.
- note: 감사 여부·조정 근거 같은 단서를 그대로 옮깁니다(예: "25년 재무자료는 未감사 기준",
  "조정 EBITDA는 일회성 비용 조정"). 이 절에서 이 줄은 빠뜨리면 안 되는 값입니다.`,

  valuation: `[valuation] Valuation
- bullets: 밸류에이션 논리를 줄로 세웁니다. 최대 ${LIMITS.valuationBullets}줄, 한 줄 140자 이내.
- **가정과 결과를 한 줄 안에 함께 적습니다**(예: "'27년 정상화 가정 · 영업이익률 20% 적용 시 EBITDA 120억",
  "EV/EBITDA 8배 적용 시 EV 960억"). 결과만 적으면 확정된 값으로 읽힙니다.
- 문서에 밸류에이션 언급이 없으면 [] 로 둡니다. 배수·시장 관행을 끌어와 계산하지 않습니다.`,

  highlights: `[highlights] 핵심 투자 포인트
- 묶음 최대 ${LIMITS.highlights}개. 각 묶음은 title(포인트 제목)과 bullets(근거 줄)로 이뤄집니다.
- title: 그 포인트를 한 마디로. 12자 이내 명사구(예: "압도적 시장 지배력", "규제 테일윈드").
- bullets: 그 제목을 뒷받침하는 사실. 묶음마다 최대 ${LIMITS.highlightBullets}줄, 한 줄 140자 이내.
  **줄마다 수치나 확인 가능한 사실이 하나는 있어야 합니다.** 없으면 그 줄을 지웁니다.
- 앞 절에 이미 담긴 숫자를 다시 인용해도 됩니다 — 이 절은 새 사실이 아니라 **왜 사야 하는가**를
  묶는 자리입니다. 다만 앞 절과 숫자가 어긋나서는 안 됩니다.
- 포인트는 문서가 세운 순서를 따릅니다. 문서가 순서를 정하지 않았으면 시장 지위 → 사업 구조 →
  생산·공급 → 규제·외부 환경 → 확장성 순으로 세웁니다.`,
}

/**
 * 체크된 절의 지시만 이어 붙인다. 고르지 않은 절의 지시를 함께 보내지 않는 이유는 비용이
 * 아니라 결과다 — 지시가 있으면 모델은 채우려 하고, 화면이 쓰지 않을 값을 만드느라 정작
 * 고른 절의 근거 탐색이 얕아진다.
 */
export function buildPrompt(cards: CardKey[], subject: string): string {
  // 절 순서는 요청 순서가 아니라 문서 순서로 고정한다. 요청 순서를 그대로 쓰면 같은 조합인데
  // 담당자가 체크한 차례에 따라 프롬프트가 달라진다.
  const ordered = CARD_KEYS.filter((k) => cards.includes(k))
  const sections = ordered.map((k) => CARD_PROMPTS[k]).join('\n\n---\n\n')
  const names = ordered.map((k) => `${k}(${CARD_LABELS[k]})`).join(', ')

  return [
    ROLE_RULES,
    FORMAT_RULES,
    ENVELOPE_RULES,
    `매각 대상 기업: ${subject || '(문서에서 확인)'}\n채울 절: ${names}`,
    '--- 절별 지시 ---',
    sections,
  ].join('\n\n')
}
