# 현재 서비스 상태 (Current Status)

구현·검증·운영 준비도의 요약입니다. 문서별 소유 범위는 다음과 같습니다.

| 질문 | 문서 |
| :--- | :--- |
| 지금 어디까지 되어 있고 무엇이 막혀 있나 | **이 문서** |
| 앞으로 무엇을 어떤 순서로 하나 | [MASTER_PLAN.md](./MASTER_PLAN.md) |
| 보안 관점의 진단과 조치 | [SECURITY_REVIEW.md](./SECURITY_REVIEW.md) |
| 어떻게 개발·검증·배포·복구하나 | [OPERATIONS.md](./OPERATIONS.md) |
| 지금 화면·라우트가 실제로 무엇인가 | [0_as_built_reverse_spec.md](./docs_planning/0_as_built_reverse_spec.md) |
| 어떤 작업을 언제 했나 | [PROGRESS.md](./docs_master/PROGRESS.md) |

* **기준일**: 2026-09-14 (Asia/Seoul)
* **기준 브랜치·커밋**: `main` / `3e8a194d` **+ 현재 작업 트리(미커밋 포함)**
* **기준 범위**: 소스·설정을 읽고 로컬 명령을 실행해 얻은 결과입니다. **커밋되지 않은 상태이므로 기준선이 아닙니다** — 같은 트리에 다른 작업자의 진행 중인 변경(녹취 구현 등)도 함께 있습니다.

> [!IMPORTANT]
> 아래 GUEST 절의 수치는 **2026-09-14 현재 작업 트리**에서 측정한 값입니다. §0·§3의 종전 수치는 역사 기록이며 이번 GUEST 독립 계정 결과를 대체하지 않습니다. 확인하지 않은 것: GitHub 워크플로 실행 이력, 실제 배포, 운영 쓰기, 실제 계정 브라우저 E2E.

> [!WARNING]
> 본 문서는 배포 완료를 선언하지 않습니다. 운영 URL, 운영 담당자, 운영 Supabase 프로젝트와 로컬 마이그레이션의 정합성은 저장소만으로 확정할 수 없습니다. **마이그레이션 파일이 있다는 사실은 운영 DB에 적용되었다는 증거가 아닙니다.**

---

## GUEST 독립 계정 정리 — 로컬 구현·검증 완료, 운영 미적용

* **확정 정책**: GUEST 프로필은 이름·소속·이메일만 취급합니다. GUEST 전화번호와 계정-원장 관계는 저장·조회·검색·인증·알림에 사용하지 않습니다. NETWORKS 불러오기는 전용 축약 RPC가 세 값만 입력 폼에 복사하며 선택 행 ID를 생성 요청에 싣지 않습니다. 직원·업무 원장 연락처, 계정·비밀번호 해시, 프로그램/FUND 참여, 사업 명부와 제출 기록은 보존합니다.
* **DB 구현**: 후속 마이그레이션 `20260914230000_guest_independent_account_cleanup.sql`이 기존 GUEST의 `users.phone/company_id`, 참여 행의 `master_table/master_id`, 초대 행의 프로필·연락처·원장·옛 자격증명 복사값을 정리하고 재발 방지 제약·트리거를 둡니다. `guest_identities`와 live 함수 의존성은 제거하되 `program_participant_entries`와 비-GUEST 관계는 유지합니다. 단건·일괄 생성, 목록, ADMIN 수정, 접근 개방과 후보 조회 RPC도 독립 계정 계약으로 교체했습니다.
* **앱 구현**: Edge 로그인·갱신·초대·재설정은 전화번호 fallback과 원장 인격 동기화를 사용하지 않습니다. GUEST 앱 세션/화면과 WORKS 계정 목록·등록·수정·사업 계정 선택은 이름·소속·이메일 계약으로 맞췄습니다. 목록의 수동 `No.` 열을 제거해 공용 `DataTable` 번호 열 하나만 남겼고, 공용 컴포넌트·토큰은 변경하지 않았습니다.
* **데이터 전환 검증**: 빈 DB에서 전체 **394개 마이그레이션**을 재생했고, `20260914220000` 상태에 레거시 픽스처를 넣은 뒤 후속 마이그레이션을 실제 적용해 계정 역할·이름·이메일·소속·비밀번호 해시·참여·사업 명부·초대 bookkeeping 보존과 제거 대상 0건을 확인했습니다. GUEST/ACL/RLS 표적 pgTAP은 **11파일·212단언**(신규 독립 계정 45단언) 전부 통과했습니다.
* **코드·화면 검증**: GUEST Vitest **9파일·248건**, Edge 표적 **5파일·79건**, WORKS 표적 **8파일·132건**, 세 영역 타입 검사가 통과했고 WORKS 프로덕션 빌드도 통과했습니다. 목 데이터 브라우저 검수는 등록·오류·원장 선택·읽기 거절·대량등록을 375/768/1280px에서 **15/15 통과**했으며 디자인 시스템 이탈과 가로 페이지 넘침을 발견하지 못했습니다. 실계정 E2E는 아닙니다.
* **보안 인벤토리**: 최신 스키마로 카탈로그·소스 접근·RPC 도달성·ACL 근거/결정을 다시 생성했습니다. 표 164개 모두 RLS가 켜져 있고 신규 RPC는 `authenticated`만 실행 가능하며 GUEST 거절 경로를 테스트합니다. 외부 입력으로만 관리되는 `edge-source-findings.json`·`edge-source-inventory.json`은 아직 옛 `guest_identities` 흔적을 담아 후속 갱신이 필요합니다.
* **남은 비관련 실패**: 전체 DB 테스트에서 진행 중인 파일받기 테스트 13건(`file_collection_isolation` 11, `file_collection_structure_save` 2)과 기존 `ledger_bulk_hard_delete_test.sql` 126행 문법 오류가 남습니다. GUEST/ACL/RLS 묶음은 통과했으며 이 실패를 이번 정책 완료로 숨기지 않습니다.
* **운영 적용 순서**: 별도 승인 후 ① 적용 직전 GUEST/참여/초대/매핑 건수와 복구용 백업 확인 ② 새 Edge 인증·초대·재설정 함수 배포 ③ DB 마이그레이션 적용 및 제거/보존 건수 대조 ④ WORKS와 GUEST 클라이언트 배포 ⑤ 구 서명 호환 래퍼 제거는 모든 구형 클라이언트 종료를 확인한 별도 마이그레이션에서 수행합니다. DB 단계는 개인정보와 매핑을 실제 삭제하므로 백업 없이 적용하지 않습니다.
* **미수행**: 운영 DB 변경, Edge·프론트 배포, 커밋·푸시는 하지 않았습니다. `pnpm db:baseline:check`는 cutoff 이하 380개 해시 일치·이후 14개로 통과했습니다.

---

## 0. 2026-09-13 출시 점검 (역사 — Codex 직접 실행)

아래는 Codex가 당시 **직접 실행·조회해 얻은 결과**입니다. 현재 GUEST 수치는 위 독립 계정 절이 답하며, 이 절과 §3 이하는 해당 시점의 이력으로 남깁니다.

* **정적 게이트 전부 PASS**: `lint`(오류 0건, 경고 44건) · `typecheck` · `typecheck:functions` · `test` · `build`. 테스트는 WORKS **1,140개**, GUEST **165개**, `test:baseline` **94개 파일**입니다.
* **베이스라인 갱신 PASS**: 산출물을 새로 만들고 **별도의 새 스택**에서 `verify`까지 통과했습니다. cutoff는 `20260913200000`, 마이그레이션 **380건**입니다.
* **GitHub 실행 이력 조회됨**(로그인 상태): 종전 CI `34664246997`은 **환경 변수 누락으로 실패**했고 그 원인을 수정했습니다. 종전 Deploy `34664246998`은 **`aws-region` 누락**으로 실패했습니다.
* **Actions 설정 비어 있음**: Repository와 `production` 환경 모두 **Secrets·Variables가 하나도 없습니다**. 값을 채우기 전에는 빌드·배포가 같은 원인으로 다시 실패합니다.
* **AWS 로컬 자격 권한 부족**: CloudFront `ListDistributions`와 IAM `ListRoles` 권한이 없어 배포 대상·역할을 로컬에서 확인하지 못했습니다.
* **운영 DB는 읽기만 수행**: `20260913180000`까지 적용된 것을 확인했고, **예산 관련 2건은 미적용**입니다. **운영 DB 쓰기와 배포는 실행하지 않았습니다.** (이 점검 이후 같은 날 파일받기 2종의 DB 적용과 Edge 배포가 별도로 승인·수행되었습니다 — §3.4. 예산 2건은 여전히 미적용입니다.)
* **커밋·푸시는 사용자가 전체 승인**했습니다. 다만 **새 CI·배포가 성공했다는 주장은 아직 하지 않습니다** — 실행 결과를 확인한 뒤에 적습니다.

---

## 1. 전체 판단

주요 화면, 도메인 스키마, RLS 구조, 단위 테스트 기반이 폭넓게 구현되어 있습니다. 2026-09-13에는 **전사 GUEST 계정 생성·조회 화면, ADMIN 계정 수명주기·연락처·비밀번호 초기화, 부분 성공 대용량 생성, PROJECT·M&A·FUND 명부 추가·제외 분리**가 저장소에 반영되었습니다. 사업부가 운영하는 `programs` 원장의 사용자 표기는 메뉴부터 상세까지 **프로젝트**로 통일했습니다. M&A 계정 존재 숨김과 직접 명부 쓰기 우회도 DB 경계에서 차단합니다.

**정적 게이트와 DB 회귀는 2026-09-13 실행에서 전부 통과합니다**(수치·범위는 §3). 그 통과는 **로컬·격리 환경의 것**이며 운영 환경이나 GitHub 실행 이력을 말하지 않습니다.

남은 것은 성격이 둘입니다. ① **저장소 밖 증거가 필요한 것** — GitHub 워크플로 실행, 실제 배포, 운영 DB 반영, 운영 CORS, 관측성, 브라우저 E2E. ② **작업 트리 안에서 아직 닫히지 않은 것** — 실발송(어댑터 없음, `SEC-2`), 역할×워크스페이스 **전역 권한 감사**와 연결된 후속(쓰이지 않는 레거시 ACL, `open_program_guest_access` 범위 불일치, 저장소에 없는 `guest-temp-resolve`), 신규 환경 부트스트랩 경로. 현 단계는 여전히 **통합 검증 전 단계**입니다 — 승인된 안정화 범위의 구현과 테스트는 검토를 마쳤고(§3), 위 후속은 그 범위 밖입니다.

---

## 2. 영역별 상태

| 영역 | 현재 구현 | 남은 것 |
| :--- | :--- | :--- |
| WORKS | 스위처 8항목의 화면과 원장·업무 흐름. `/guest-accounts` 한 화면을 사업부·M&A팀·투자실 하단에서 열며 일반 사용자는 생성·조회, ADMIN은 정지·해제·삭제·연락처·비밀번호를 관리 | 브라우저 경로 검증 |
| GUEST | 이메일+비밀번호 커스텀 JWT 로그인, 맥락(프로젝트/FUND) 선택·**전환**, 고정 메뉴 3종(사업개요·공지사항·Q&A — 일정안내는 2026-09-13 한시 철회, §3.3) + 모듈 화면, 공개 링크 3종. 전환 목록을 화면이 구독해 로그인 직후에도 바로 갈아탈 수 있고, 선택 화면·전환기·개요 요약이 종류·자격을 같은 어휘로 세운다(§3.2). Vitest 러너 편입(인증·세션·비밀번호 규칙 + 맥락 표시 규칙 + `richText` 정화기 회귀) | 안내 메일·재설정 링크의 실발송, 브라우저 경로 검증 |
| Supabase | 순차 마이그레이션 **374건**·Edge Functions·회귀 SQL. 격리 스택에서 전수 재생과 pgTAP이 통과하고(§3), GUEST 계정 중복·수명주기·비밀번호·명부·M&A 가시성 경계가 회귀로 고정됨 | 역할×워크스페이스 **전역 권한 감사**, 운영 반영 여부 확인, 신규 환경 부트스트랩 경로 검증 |
| 공통 UI | 디자인 토큰·밀도 체계와 ESLint 회귀 방어 규칙(`eslint.config.mjs`의 `designSystemRules`) | GUEST 잔여 수제 UI 편입 |
| CI | `main`·PR에서 lint → typecheck → `typecheck:functions` → build → test + 재사용 `DB 회귀` 잡. 근거: `.github/workflows/ci.yml`, `db-tests.yml` | **GitHub 워크플로 실행 이력 미확인** — 같은 명령·같은 러너의 로컬 결과는 §3에 있습니다 |
| 배포 | CI 완료(`workflow_run`)·수동 실행만 트리거이고, `gate` 잡이 **같은 SHA의 CI 성공**을 확인해 아니면 차단(fail-closed). 근거: `.github/workflows/deploy.yml` | **실제 실행·배포로 확인하지 않음** — 오프라인 시나리오 26건과 YAML 파싱만 확인(`OPS-1`) |
| 운영·복구 | RPO 24시간/RTO 4시간 목표의 복구 런북(`infra/runbooks/restore-drill.md`) | 실제 리허설 수행 기록 미확인 |

---

## 3. 현재 검증 결과

**최신 실행**: 2026-09-13 · **대상**: `main` / `846dfb83` + 현재 작업 트리(미커밋) · **환경**: 로컬·격리 스택. **운영 DB·GitHub 실행 이력은 대상이 아닙니다.**

| 명령 | 결과 |
| :--- | :--- |
| `lint` | exit 0 — 오류 0건, 경고 44건(기존 경고) |
| `typecheck` · `typecheck:functions` | exit 0 |
| `build` | exit 0 — 4/4 |
| WORKS·GUEST Vitest | exit 0 — **94개 파일 / 1,233개 테스트**(WORKS 1,071 + GUEST 162) |
| `test:baseline`(2026-09-12 기록) | exit 0 — **94개 통과, 건너뜀 0** |
| `test:db`(격리 스택, 신규 DB) | exit 0 — 마이그레이션 **375건** 재생, pgTAP **20개 파일 / 단언 528개 전부 통과**(저장소 19개 + 러너 생성 1개) |
| GUEST 계정 표적 회귀 | exit 0 — WORKS **9개 파일 / 166개**, DB 연락처·대량생성·명부 **3개 파일 / 145개** |
| 인가 인벤토리 회귀 표적 재실행 | exit 0 — `authorization_inventory_test.sql` **계획 56 / 실행 56**(거절 단언 `C23`의 메시지를 실제 인가 응답과 맞춘 뒤) |
| `db:baseline:verify` | exit 0 — **산출물을 만든 스택이 아닌 새 스택**에서 재생한 덤프가 스냅샷과 **바이트까지 동일** |

* GUEST의 162개에는 `richText` 정화기 회귀 124개가, WORKS에는 닫힌 알림 발송 창구 회귀가 들어 있습니다.
* 통과한 단언은 **그 단언이 통과했다는 뜻**이며 해당 기능 전체가 검증되었다는 뜻이 아닙니다. DB 결과는 **격리된 신규 DB 기준**이라 운영 DB의 적용 상태·거동을 말하지 않습니다.
* **실행하지 않은 것**: GitHub 워크플로 실행·배포, 운영 Supabase·운영 DB 조회, 브라우저 E2E.
* 위는 **승인된 안정화 범위**에 대한 결과이며, 그 범위의 구현과 테스트는 검토를 마쳤습니다. 범위 밖 후속은 §4와 [MASTER_PLAN.md](./MASTER_PLAN.md)가 가집니다.

### 베이스라인 산출물 — 2026-09-13 03:23 KST

`supabase/baseline/`에 `current_schema.sql`·`manifest.json` **한 쌍이 있습니다**(종전 "미생성" 서술은 더 이상 사실이 아닙니다). manifest가 답하는 값은 cutoff `20260913160000`, 마이그레이션 375건(cutoff 이후 0건), `migrationHistorySha256` `d97577d3…`, `schemaSha256` `0cb2de01…`, 생성 환경 PostgreSQL 17 / Supabase CLI 2.109.0, `source`는 격리 로컬 스택입니다.

* **소유 경계**: cutoff·마이그레이션 수·해시·생성 환경 같은 **값은 `manifest.json`**이, **생성·검증 절차와 한계는 [12_database_baseline_operations.md](./docs_dev/12_database_baseline_operations.md)**가, **실제로 돌린 검사의 결과는 이 문서 §3**이 가집니다. 위 값은 manifest를 읽어 옮긴 것이며 해시 표를 따로 두지 않습니다.
* 스냅샷이 보증하는 것은 **cutoff 시점 스키마의 재현성 하나**입니다 — **신규 환경 부트스트랩·복구가 된다는 증명이 아니고**, 담기는 범위도 `public`·`app` 두 스키마뿐입니다.
* **독립 재검증이 통과했습니다** — 산출물을 만든 스택이 아닌 **새 스택**에서 재생한 덤프가 스냅샷과 바이트까지 같습니다(§3).
* 산출물은 **작업 트리에만** 있습니다. 커밋·배포·운영 DB 반영은 하지 않았습니다.

### 1차 게이트 기록 (역사 — 그때의 트리)

아래는 지우지 않고 요약으로 남기는 **과거 시점의 값**이며, 현재 상태는 위 최신 실행이 답합니다.

| 시각 | 결과 |
| :--- | :--- |
| 11:45~11:47 | 전체 JS 게이트 exit 0 — 82개 파일 / 905개 테스트, `build` 4/4 |
| 14:59 | DB 회귀 exit 0 — 마이그레이션 363건 재생, pgTAP 12개 파일 / 단언 224개 통과 |
| 15:49 | GUEST 부분 집합 exit 0 — 3개 파일 / 162개 테스트(정화기 회귀 124개 포함) |

### 3.1 권한 인벤토리의 요지

표 단위 결정·근거·수치의 정본은 [15_authorization_inventory.md](./docs_dev/15_authorization_inventory.md)와 `supabase/security/`의 산출물, 그리고 각 마이그레이션의 머리 주석입니다. **여기에 같은 표를 다시 두지 않습니다.** 판단에 필요한 요지만 남깁니다.

* **`authenticated`는 임직원과 게스트가 함께 쓰는 한 롤입니다** — 게스트 세션 JWT가 `role: "authenticated"`로 서명되므로 `grant … to authenticated` 한 줄은 외부 참여자에게도 열리고, 둘을 가르는 것은 **오직 RLS 술어**입니다. 최소 권한 판단은 임직원 위협모형만으로 정당화되지 않습니다.
* 격리 재생 기준으로 표 **159개**, `SECURITY DEFINER` 루틴에 **`search_path` 누락 0건**, 브라우저 접촉면은 표 103개·RPC 80종·Edge Function 22개입니다.
* `service_role`에 **모든 표에서 DML이 없다**는 관측은 **보정 전(2026-09-12 초기 재생) 시점의 값**이며 현재 상태가 아닙니다 — 보정 이후의 역할별 권한은 [15_authorization_inventory.md](./docs_dev/15_authorization_inventory.md)와 `supabase/security/acl-decisions.json`이 답합니다.
* ACL 보정은 마이그레이션 **4건**입니다 — 테이블 권한·기본 권한·RPC `EXECUTE` 좁히기에 이어, `20260912161500`이 `guest_invitations`의 `INSERT`·`UPDATE` 정책에 **내부 사용자 조건을 AND로** 더합니다. 표 권한이 열린 회차에 **현실적인 게스트 조합(`guest`·`write`·`self`)이 실제로 통과하는 것**이 회귀에서 드러났고, 지금은 **거절됨이 회귀로 고정**되어 있습니다.
* **이 보정이 전자결재 목록을 한 차례 끊었습니다(2026-09-13 확인·복구)**: `20260912160000`이 `approval_recipients`·`approval_reads`에 쓰기만 남기고 `SELECT`를 걷었는데, 두 표는 `approval_documents` 조회에 임베드되어 함께 읽힙니다. **표 권한은 RLS보다 먼저 검사되므로** 요청 전체가 `42501`(HTTP 403)로 끝났습니다 — RLS 거부였다면 빈 200이었을 것입니다. 두 표의 `SELECT` 정책은 멀쩡했고 빠진 것은 `GRANT` 하나였습니다. `20260913170000`으로 복구해 운영 DB에 적용했고, 같은 형태(쓰기 권한은 있으나 읽기 권한이 없는 표)는 운영 DB 전체에서 이 둘뿐임을 확인했습니다.
* 이 보정은 **게스트 로그인 정책을 건드리지 않습니다** — 이메일 아이디 + 비밀번호, 설정 전에만 유효한 초기 연락처, 자격증명의 오프라인 취급은 그대로입니다(§4).
* **이 정리는 서비스 전체의 권한 감사가 아닙니다.** 전역 역할×워크스페이스 감사, 오래되어 쓰이지 않는 ACL 정리, `open_program_guest_access`의 지원 범위(FUND·M&A)와 `guest_invitations` 정책 범위(PROJECT·GUEST)의 불일치, 저장소에 없는 `guest-temp-resolve`는 **연결된 후속 과제**로 남습니다(§4, [MASTER_PLAN.md](./MASTER_PLAN.md)).

### 3.2 GUEST 맥락 전환·개요 개편 (2026-09-13, Claude 실행 · **Codex 리뷰 완료 — 코드·검사 한정**)

§0·§3 표의 수치는 이 변경 **이전**의 트리를 잰 값입니다. 아래는 그 뒤에 `apps/guest`에서 수행한 작업과 **직접 실행해 얻은 결과**이며, 범위·판단 근거는 [PROGRESS.md](./docs_master/PROGRESS.md) Phase 29가, 화면 요건은 [3_9_workspace_guest.md](./docs_planning/3_9_workspace_guest.md) §1.0·§1.1이 소유합니다.

* **고친 것**: 전환 목록을 화면이 구독하도록 바꿔(질의 키 `['guest','me']` 한 벌) 로그인 직후에도 전환기가 서게 했고, 세션 갱신이 지우던 참여 자격(persona)을 잇고, 갈아탄 뒤 늦게 도착한 갱신 응답·401이 새 세션을 덮거나 끊지 못하게 막고, 맥락이 바뀌면 이전 맥락의 조회 캐시를 버리게 했습니다(로그아웃은 캐시 전체). 화면은 선택 카드·전환기·개요 요약 카드입니다. **서버 계약·권한·로그인 정책은 건드리지 않았습니다.**
* **실행 결과(전부 exit 0)**: `pnpm lint`(오류 0 · 경고 44, 기존과 같음) · `pnpm typecheck` · `pnpm test`(WORKS 94파일 1,142건 + GUEST 5파일 **186건** + `test:baseline` 94건) · `pnpm build` 4/4.
* **리뷰 1차 반영(2026-09-13)**: 펼친 목록의 높이를 화면에 묶어 안에서 굴리게 하고(참여가 많으면 아래 줄에 닿을 길이 없었습니다), 고르는 자리의 이름을 자르지 않고 줄바꿈으로 세우고(앞머리가 겹치는 사업명이 터치 환경에서 갈리지 않았습니다), 목록의 '현재' 표식을 수제 배지에서 글자로 내리고, 전환이 끝난 뒤 초점이 트리거(성공)·누른 줄(실패)로 돌아오게 했습니다 — 요청 중에는 두 버튼이 모두 `disabled`라 그 자리의 `focus()`가 아무 일도 하지 않았습니다.
* **추가 검증(직접 실행, exit 0)**: `apps/guest`에서 `vitest run` **6파일 191건**(신설 `src/app/GuestContextSwitcher.test.ts` 5건 포함) · `tsc --noEmit` · 변경 파일 `eslint`(지적 0) · `vite build`. 신설 회귀는 실제 컴포넌트를 `MemoryRouter` + 실제 `queryClient`·스토어 위에 얹고 `fetch`만 대역으로 세운 jsdom 통합 시험이며, **세 가지 되돌림에서 실제로 빨개짐을 확인**했습니다: 전환기의 질의 구독 제거(2건 실패) · `enterContext`의 캐시 리셋 제거(3건) · 초점 복귀 효과 무력화(2건). 확인 뒤 원상복구했습니다.
* **미실행·미검증**: 브라우저 경로(`E2E-1`) — 이 저장소에는 헤드리스 브라우저(Playwright·Puppeteer)가 설치되어 있지 않아 **좁은 폭/데스크톱의 실제 렌더는 보지 못했고, 살아 있는 인증 백엔드를 거친 E2E도 수행하지 않았습니다.** 따라서 목록 높이 제한(`max-h-[60vh]` + 스크롤)과 줄바꿈이 실제 화면에서 어떻게 보이는지는 미검증이며, 검증한 것은 DOM 동작·상태·초점까지입니다. **이 둘은 통과가 아니라 공백입니다.** 운영 DB·배포·Edge Function은 이 변경의 대상이 아니며 손대지 않았습니다.
* **리뷰(2026-09-13, Codex 최종)**: 1차 지적 반영분까지 Codex가 **독립적으로** 확인했습니다 — `pnpm --filter @ynarcher/guest test`(6파일 191건, exit 0) · `pnpm --filter @ynarcher/guest build`(`tsc --noEmit` + vite, exit 0, 번들 크기 경고만) · 변경·신설 GUEST TS/TSX **12개 파일 전부**에 `eslint`(exit 0, 지적 0). 코드·디자인 시스템 사용(`Card`·`CardShell`·`Badge`·`Dropdown`·`InfoGrid`와 토큰), 선택 목록의 줄바꿈, 높이 제한과 스크롤, 초점 복귀, 그리고 실제 통합 테스트의 내용까지 직접 보았고 **남은 차단성 지적은 없습니다.** 리뷰 범위는 코드와 자동 검사까지이며, 위 미검증 두 항목은 **이 리뷰로 해소되지 않습니다.**

### 3.3 GUEST 일정안내 철회 (2026-09-13, Claude 실행 · **Codex 리뷰 완료 — 코드·자동 검사 한정**)

사용자 지정으로 GUEST의 `일정안내`를 **모든 페르소나·맥락에서 걷었습니다**(한시 철회 — 되살리는 조건은 사용자 지시입니다). 화면 요건의 정본은 [3_9_workspace_guest.md](./docs_planning/3_9_workspace_guest.md) §1.1이며 그곳에 종전 설계를 기록으로 남겼습니다.

* **고친 것**: 고정 메뉴에서 `/schedule` 줄을 뺐고(프로젝트·M&A 묶음, FUND는 2026-09-09부터 이미 없었습니다), 게스트 전용 화면 `apps/guest/src/pages/SchedulePage.tsx`를 지웠고, 옛 경로 `/schedule`은 기존 `GuestEntry` 리다이렉트를 재사용해 `/overview`로 보냅니다(즐겨찾기·옛 링크가 404로 떨어지지 않게).
* **건드리지 않은 것**: WORKS의 일정 기능(사업 상세 프로그램 탭 간트)과 공용 부품 `@ynarcher/ui`의 `ScheduleBoard`, 그리고 원장·RPC·Edge Function·RLS는 그대로입니다. 걷은 것은 **GUEST의 진입 경로와 그 화면 하나**뿐입니다.
* **실행 결과(직접 실행)**: `apps/guest`에서 `vitest run` **6파일 192건 exit 0**(고정 메뉴 3종·`/schedule` 부재를 맥락 4종에 대해 고정하는 회귀 1건 추가) · `tsc --noEmit` exit 0 · `vite build` exit 0(기존 번들 크기 경고만) · 변경 파일 4개 `eslint` **오류 0**(router.tsx의 `react-refresh` 경고 1건은 종전과 같은 기존 경고).
* **리뷰 지적 반영**: `navigation.ts`의 새 주석이 "모든 맥락에 빈 화면 문제가 있다"는 근거를 지어냈고 "FUND 묶음도 접었다"고 사실과 다르게 적은 것을 걷어, 사용자 지정에 따른 공통 한시 철회와 현재 고정 메뉴 셋만 남겼습니다(`FUND_FIXED_NAV`는 그대로 있습니다). `GuestLayout.tsx`의 `사업은 넷, 조합은 셋` 주석, `README.md`와 [CLAUDE.md](./docs_master/CLAUDE.md)의 GUEST 라우트 목록에서 `일정` 항목도 함께 정리했습니다.
* **리뷰(2026-09-13, Codex 최종)**: 주석 수정까지 포함한 최종 diff를 Codex가 보고 **차단성 지적이 없습니다.** 독립 실행 근거는 `pnpm --filter @ynarcher/guest test`(6파일 192건, exit 0) · 게스트 빌드(`tsc --noEmit` + `vite build`, exit 0) · 손댄 5개 파일 `eslint`(`navigation.ts`·`navigation.test.ts`·`router.tsx`·`guestStore.ts`·`GuestLayout.tsx`, exit 0, router의 기존 fast-refresh 경고 1건만) · 스테이지·언스테이지 `git diff --check`(exit 0)입니다. 공용 `Sidebar`/`SidebarItem`의 스타일과 토큰이 그대로임도 확인했으며, 디자인 시스템 확인은 **코드 대조까지**입니다.
* **미실행**: 저장소 전체 `pnpm lint`/`pnpm test`/`pnpm build`, 커밋·푸시·배포. **브라우저에서의 실제 이동과 시각 확인은 하지 않았습니다**(`E2E-1`) — 이 리뷰로 해소되지 않는 공백입니다.

### 3.4 파일받기(FILE_COLLECTION) 모듈 (2026-09-13, 구현·리뷰 완료 — **운영 DB·Edge 반영 완료**)

계약·화면·검증의 정본은 [3_4_16_file_collection.md](./docs_planning/3_4_16_file_collection.md)이며, 여기서는 사실과 공백만 둡니다.

* **구현된 범위**: PROJECT·M&A 두 워크스페이스 전용(**FUND는 사용자 확정으로 이번 범위 제외**), ADMIN 카탈로그 토글로 켜는 `FILE_COLLECTION` 모듈. 깊이 상한 없는 폴더·문항 트리, 게스트 계정 단위 배정과 **게스트 간 완전 격리**, 문항 단위 파일 업로드·댓글·제출·승인/보완요청과 **회차 이력 보존**. 화면은 카드·표 모두 공용 디자인 시스템 부품을 씁니다.
* **순서 이동은 원자적 RPC로 확정**: `file_collection_reorder_node(p_node_id uuid, p_direction text)` → `boolean`. collection 행을 잠그고 형제 맞바꿈과 `1..n` 재번호를 한 UPDATE로 끝내며 양 끝에서는 `false`를 답합니다. WORKS 훅이 이 RPC 하나만 부릅니다(종전 형제 반복 호출 방식은 없습니다).
* **독립 검증(Codex 직접 실행, 통과)**: 루트 `test`(WORKS 98파일 **1,232건** · GUEST 9파일 **236건** · `test:baseline` 18스위트 **94건**) · 루트 `lint` **오류 0·경고 46** · `typecheck` 6개 · `build` 4개 · `typecheck:functions` · Edge 단언 **56건** · 격리 DB 러너 **22파일 693단언**(파일받기 격리 **113**, `authorization_inventory_test.sql` 파일 전체 **56** 포함) · 베이스라인 해시 기존 380건 불변·신규 2건 · 픽스처 브라우저 러너 다섯 폭 **60/60 콘솔 오류 0**(좁은 폭 결함 수정 뒤 WORKS 재실행 **20/20**). 수치·보고서 경로의 정본은 [3_4_16 §8](./docs_planning/3_4_16_file_collection.md)입니다.
* **운영 반영(완료)**: 마이그레이션 2종을 연결된 works 운영 프로젝트에 각각 별도 트랜잭션으로 적용하고, Edge Function `file-collection-file`을 배포했습니다(ACTIVE). 적용 대상·동일성 대조·권한 확인·실동작 탐침·스냅샷의 정본은 [3_4_16 §9](./docs_planning/3_4_16_file_collection.md)입니다. **프론트엔드 AWS 배포는 이번 범위가 아니며**(사용자 확정) 화면 확인은 로컬에서 했습니다. 커밋·푸시는 하지 않았습니다.
* **공백(통과가 아닙니다)**: 실제 인증 계정의 업로드 **E2E 미수행**(픽스처 브라우저는 실제 컴포넌트를 목 데이터로 렌더할 뿐입니다), 병렬 세션 **동시성 시험 미수행**(`E2E-1`). 어드바이저 점검은 `exit 1`이며 **전부 통과가 아닙니다** — 미변경 객체의 기존 `ERROR` 2건과 파일받기 RPC 15건의 의도된 `WARN`이 남아 있습니다([3_4_16 §9](./docs_planning/3_4_16_file_collection.md)).

### 3.5 파일받기 문항 구성 — 가로 계층 격자 전환 (2026-09-13, 구현·리뷰 완료 · **운영 DB 미적용**)

WORKS 파일받기의 문항 구성 편집을 품의 예산작성과 **같은 가로 계층 격자**로 바꿨습니다. 표·단계 입력·줄 조작은 공용 부품(`packages/ui/src/patterns/HierarchyTable.tsx`)을 그대로 부르며, 파일받기 고유의 것(필수·안내·저장/공개 의미)만 이 화면이 갖습니다.

* **화면**: 분류 단계 수와 단계별 이름을 정하면 각 단계가 열이 되고, 한 줄에 전체 경로와 문항을 함께 적습니다. 상위 셀은 자식 수만큼 병합되고, 칸의 `+`는 그 단계의 형제 가지를 세우며, 줄 이동·삭제는 예산표와 같습니다. 폴더·문항 창(`NodeFormModal`)을 거치지 않습니다(파일은 남아 있으나 이 탭이 부르지 않습니다).
* **저장은 한 번의 원자적 RPC**: 신규 `public.file_collection_save_structure(p_collection_id uuid, p_nodes jsonb, p_deletes jsonb, p_level_names text[], p_expected_updated_at timestamptz)` → `jsonb`. 인가·`FOR UPDATE`·공개 잠금·기존 마디 전수 버전 대조(내가 못 본 마디가 서버에 있으면 `40001`)·페이로드 검증을 모두 통과한 뒤 **삭제 → 분류(위→아래) → 문항(자리 먼저·종류 마지막)** 순서로 적용합니다. 기존 마디의 id는 보존되고 삭제는 **명시된 것만** 소프트 삭제합니다. 반환값에 저장 직후의 트리 전부(새 id·기준 시각)를 실어 화면이 조회를 기다리지 않고 기준을 다시 세웁니다.
* **단계 이름은 원장에 남습니다**: `file_collections.level_names text[] not null default '{}'`. 비어 있으면 화면이 기본 이름(`대분류…문항`)을 세우고, 그 기본값은 다른 변경과 함께 저장됩니다.
* **깊이 조작**: 단계를 늘리면 **기존 문항 앞에 빈 분류 줄을 끼워** 문항의 id·이름·안내·필수를 마지막 단계에 그대로 둡니다. 줄이기는 방금 늘리며 생긴 빈 통로(저장 전·이름 없음·외자식)만 걷어낼 수 있고, **원장에 저장된 분류가 걸리면 줄이지 않고 이유를 말합니다** — 여기서만 예산표와 다릅니다. 예산표의 분류는 글자라 줄이면 글자만 사라지지만 파일받기의 분류는 마디여서, 열에서 사라지면 화면에서 읽지도 저장하지도 못한 채 잃기 때문입니다. 옛 빈 폴더는 종류를 바꾸지 않고 '문항 넣기'로 가지를 완성합니다. 들쭉날쭉한 옛 트리(최상위 문항 + 깊은 가지)는 그대로 섭니다. **단계 수 상한은 두지 않습니다.**
* **읽지 못한 구성은 고치지도 저장하지도 않습니다**: 조회 실패·초기 로딩·상한 절단(`truncated`)·상위를 찾지 못한 마디가 있으면 편집·저장·공개를 함께 막고 읽던 것은 그대로 보여 줍니다. 저장 실패는 초안을 지우지 않으며(원자적이라 그대로 다시 눌러도 가지가 두 번 생기지 않습니다), 늦게 도착한 옛 조회 응답은 기준으로 삼지 않습니다.
* **실제 수행한 검증(2026-09-13)**: 격리 DB 러너 `file_collection_structure_save_test.sql` **54단언** + 기존 `file_collection_isolation_test.sql` **113단언** + `authorization_inventory_test.sql` 통과 · WORKS 단위 `structureDraft`/`structureEditor` **83단언**(파일받기 4파일) · 루트 `lint` 오류 0(경고 46, 기존과 동일) · `typecheck` 6개 · `build` 4개 · 픽스처 브라우저 러너 구성 화면 **45/45**(다섯 폭 × 9줄: 늦은 조회·단계 늘리기·형제 가지·빈 폴더 완성·저장 후 재편집 저장·저장 거절·읽기 전용·공개 잠김). 좁은 화면(320·375) 가로 밀림 결함 1건을 이 러너가 잡아 조작부를 카드 머리에서 본문으로 옮겨 고쳤습니다.
* **독립 검증(Codex 직접 실행, 통과)**: 파일받기·품의 Vitest **18파일 305단언** · `typecheck` 6/6 · 변경 파일 eslint exit 0 · `git diff --check` exit 0 · 격리 DB **3파일 223단언**(구성 저장 54 + 격리 113 + 권한 인벤토리 56, 프로세스 exit 0·계획 전부 소진) · 브라우저 러너 구성 화면 **45/45**(exit 0). 1280폭 스크린샷을 눈으로 확인하고 표 재사용·영속·초안/ID 처리·인가 검사를 함께 읽었습니다. 로그는 `TEMP/ynarcher-filecollection-hierarchy/`의 `codex-db-final.log`·`codex-browser-final.log`·`codex-typecheck.log`입니다.
* **공백(통과가 아닙니다)**: **운영 DB에 새 마이그레이션을 적용하지 않았습니다**(`20260913220000_file_collection_structure_grid.sql` — 적용 전에는 이 화면이 뜨지 않습니다: 조회 열과 RPC가 모두 없습니다). 커밋·푸시·배포도 하지 않았습니다. 동시 편집 경합의 **실제 병렬 실행**은 여전히 미검증이며, 브라우저 러너는 실제 컴포넌트를 목 데이터로 세운 것이라 **실계정 E2E가 아닙니다**. 구 경로(`file_collection_save_node`/`delete_node`)로 남이 방금 만든 마디를 지우면 머리 행 시각이 오르지 않아 그 조회가 옛 값으로 걸러질 수 있으며, 그때의 탈출구는 화면의 '서버 내용 불러오기'입니다 — 리뷰에서 확인된 알려진 한계이며 이 구현의 차단 요소는 아닙니다.

### 3.6 파일받기 화면 정리와 공개 판정 이관 (2026-09-13, 화면 구현 완료 · **DB 미검증·미적용**)

같은 날 §3.5 위에 이어진 사용자 지시들입니다. 중복된 자리를 걷어내고, 공개 판정을 모듈 하나로 모읍니다.

* **화면에서 걷어낸 것**: 분류 줄의 '분류 안내'(게스트 화면에 뜨지 않던 쓰기 전용 칸), 표 아래 '대분류 추가' 버튼(첫 단계 칸의 `+`와 같은 일), '받을 자료 안내' 카드(옆 칸 NOTICE와 중복), 되돌리기·공개하기 버튼, 표 아래 설명 문단과 미저장 건수 문구. 남은 조작은 **대용량 등록 · 확대보기 · 구성 저장** 한 줄입니다.
* **배치**: 파일받기도 다른 게스트 모듈과 같은 **본문 2 : NOTICE 1** 격자 안에 섭니다(`ModuleNoticeSplit`). 좁아진 폭은 표가 자기 칸 안에서 가로로 스크롤해 받고, 그래도 좁으면 **확대보기**(다른 워크스페이스와 같은 `ExpandToggleButton`+`FullscreenPanel`)가 전체 화면으로 펼칩니다.
* **표**: 문항마다 **No.** 열이 맨 앞에 섭니다(`HierarchyTable`의 `numbered`). 표의 최소 폭이 **단계 수를 따라 늘어나고**(`38rem + 단계수 × 9rem`, 2단계에서는 종전과 같은 56rem), 이름 칸에는 8rem 바닥이 생겨 단계를 늘려도 칸이 뭉개지지 않습니다. 세로로 병합된 분류 칸은 입력이 **병합 높이를 그대로 채웁니다.** 필수 칸은 `w-24`→`w-12`, 문항 안내는 `w-72`→`w-56`입니다.
* **문항 대용량 등록**: 양식 CSV를 내려받아 채운 뒤 올립니다(`양식 내려받기(CSV)` / `채운 파일 올리기`). 머리글은 화면의 표와 같은 순서(단계 이름들 · 필수 · 문항 안내)이며, 위 단계 칸을 비우면 위 줄을 잇고 이름이 같은 분류에는 붙습니다(문항은 언제나 새로 섭니다). 담기 전에 **줄마다 읽힌 칸과 못 읽은 이유를 미리보기로 세우고**, 담아도 서버에는 `구성 저장`을 눌러야 적힙니다.
* **공개 판정 이관(정책 변경)**: 파일받기 자체의 '공개하기'를 없애고 **모듈 공개 여부**가 밖으로 나가는 시점을 정합니다. 구조를 영구히 잠그던 규칙도 함께 없애고, 대신 **이미 자료를 받은 문항의 삭제·이동·종류 변경만** 서버가 막습니다. 응답 칸은 공개 시점에 몰아 만들던 것을 **배정·구성 저장 때마다 맞추는 것**으로 옮겼습니다(`app.fc_sync_responses`). `published_at`·`published_by`·`file_collection_publish()`는 기록으로 남되 어떤 판정에도 쓰이지 않습니다.
* **실제 수행한 검증**: 루트 `typecheck` 6/6 · `lint` 오류 0(경고 46, 기존과 동일) · `build` 4/4 · WORKS Vitest **102파일 1,299건**(대용량 등록 파서·트리 결합 **14건** 신규) · GUEST Vitest **9파일 236건**.
* **공백(통과가 아닙니다)**: 마이그레이션 `20260913230000_file_collection_publish_by_module.sql`은 **작성만 했고 실행해 보지 못했습니다** — 운영 DB 비밀번호가 거부되어(`28P01`) 롤백 드라이런조차 하지 못했습니다. 따라서 SQL 문법·의존성·트리거 거절 경로는 **미검증**이며 pgTAP도 갱신하지 않았습니다. §3.5의 `20260913220000`도 여전히 미적용이라 **두 건을 순서대로** 적용해야 합니다. **화면만 먼저 배포하면 안 됩니다** — 공개 버튼이 사라진 화면에 옛 서버 규칙이 남으면 어떤 파일받기도 게스트에게 나가지 않습니다. 적용 즉시 모듈이 이미 공개(OPEN·CLOSED)인 파일받기는 대상 게스트에게 보입니다(사용자 확인 완료).

### 3.7 GUEST 계정 등록 일원화와 고정 초기 비밀번호 (2026-09-14, 구현 완료 · **격리 DB 재생·pgTAP 통과** · 운영 미적용)

> 이 절은 `20260914210000`·`20260914220000`까지의 역사 기록입니다. 연락처 보존과 선택적 원장 연결에 관한 설명은 위 **GUEST 독립 계정 정리**와 후속 `20260914230000` 정책으로 대체됐습니다.

사용자 지시로 GUEST 계정의 **등록 칸·개시 비밀번호·명부 추가 창**을 한 묶음으로 바꿨습니다.

* **등록이 받는 값은 이름·이메일·소속 셋과 선택적 원장 연결뿐입니다.** 소속(`users.affiliation`)은 새로 만든 실제 칸이며 `company_id`(스타트업 FK)와 다른 축입니다. **연락처는 등록 창구에서 걷었습니다** — 수기 표·파일 템플릿·서비스 호출 어디에도 없고, 원장 선택창도 연락처를 옮겨 담지 않습니다. 이미 저장된 연락처는 지우지 않으며 ADMIN 계정 정보 수정과 명부 표시가 계속 다룹니다. 옛 템플릿의 연락처 열이 든 파일은 막지 않고 그 열만 버립니다.
* **최초 비밀번호와 ADMIN 초기화 결과는 고정값 `ynarcher`입니다.** 값은 Edge 상수 하나가 소유하고 DB는 종전대로 `password_hash IS NULL`로 개시 상태를 표현합니다. 개시 로그인은 세션이 아니라 10분짜리 설정 티켓만 받고, 본인이 개인 비밀번호를 정한 뒤에야 세션이 열립니다(변경 없음). 이미 정한 개인 비밀번호는 그대로입니다. 초기화의 연락처 전제 조건과 `uq_users_guest_phone` 유일 인덱스는 철회했습니다.
* **사업 명부의 `GUEST 계정 추가`는 좌우 이관 두 기둥을 걷고 한 벌의 표가 되었습니다.** 계정 모델 축의 탭 일곱(`전체`+여섯 분류) → `ListToolbar` → 체크박스 `DataTable` + 서버 검색·분류·페이지네이션이며, 이미 담긴 계정은 보이되 고를 수 없습니다. **이 창에서 빼지 않습니다**(빼기는 명부 표가 소유). 선택 줄의 `로그인 열기`는 `{프로젝트|M&A 프로젝트|FUND} 접근 허용`으로 이름만 바뀌었고 동작은 그대로이며, **비밀번호 재설정 안내는 명부에서 걷었습니다**(초기화 창구는 `/guest-accounts` 하나).
* **좁은 화면**: 등록 표는 접지 않고 최소 폭(40rem) 안에서 가로로 스크롤합니다 — 375px에서 칸이 40px로 눌려 적은 값이 한 글자도 보이지 않던 것을 이 규약이 고쳤습니다(파일받기 구성 표와 같은 자리).
* **마이그레이션 2건**: `20260914210000_guest_account_affiliation.sql`(소속 칸·인덱스 철회·발급/단건/일괄/ADMIN 수정/목록 RPC), `20260914220000_guest_fixed_initial_password.sql`(초기화의 연락처 전제 철회·주석).
* **실제 수행한 검증**: 격리 스택 재생 후 `node scripts/db-test/run-db-tests.mjs`(게스트 관련 7파일) — **Files=7, Tests=239, Result: PASS**, 두 신규 마이그레이션까지 전부 재생·스택 정리 성공(로그 `TEMP/guest-policy-…/db-tests.log`). 루트 `typecheck` 6/6 · `typecheck:functions` · `lint` 오류 0(경고 46, 기존과 동일) · `build` 4/4 · WORKS Vitest 102파일 1,313건 · GUEST Vitest 9파일 253건 · 베이스라인 node 테스트 94건. 브라우저 픽스처 러너로 계정 창 30장(375·768·1280)을 찍어 눈으로 확인했습니다.
* **대용량 업로드 페이지의 375px 가로 넘침은 해소했습니다.** 공용 `DetailTopBar`의 액션 자리가 `flex shrink-0`이라 버튼 넷이 한 줄을 고집해 페이지가 통째로 밀렸는데, **공용 부품을 고치지 않고 부르는 쪽에서** 폭 상한을 준 조작부 묶음(`sm` 위에서는 상한 해제)으로 좁은 폭에서만 접히게 했습니다. 세 폭 모두 `pageNoHorizontalOverflow` 통과이며 1280px의 한 줄 배치는 그대로입니다.
* **공백(통과가 아닙니다)**: **운영 DB에 적용하지 않았고 커밋·푸시·배포도 하지 않았습니다.** 브라우저 검증은 목 데이터를 쓰는 픽스처 러너이며 실계정 E2E가 아닙니다. `supabase/security/*.json`(ACL·Edge 인벤토리)은 cutoff가 2026-09-13이라 이번 시그니처 변경을 아직 담지 않았습니다.
* **보안**: 개시값이 전 계정 공통이 되어 F-0 잔여 위험의 성립 조건이 이메일 하나로 줄었습니다([SECURITY_REVIEW.md](./SECURITY_REVIEW.md) `F-0`). 사용자가 알고 내린 결정입니다.

### 테스트 범위에 대한 사실

* `pnpm test`는 turbo가 각 패키지의 `test` 스크립트를 부르며 **WORKS와 GUEST 둘 다** 돕니다.
* WORKS의 Vitest는 `src/**/*.test.ts` 외에 `../../supabase/functions/**/*.test.ts`와 `../../scripts/**/*.test.mjs`를 함께 돕니다. Edge Function의 **순수 판정과 핸들러 경계**(DB 클라이언트만 대역)가 여기서 검증되며, **Deno 런타임·PostgREST/RLS·브라우저 경로는 대상이 아닙니다.**
* GUEST의 Vitest는 `src/**/*.test.ts`만 담고, 경계로 세운 대역은 `fetch`·`localStorage`뿐입니다. 기본 환경은 `node`이며 DOM이 필요한 파일만 상단 `// @vitest-environment jsdom`으로 자기 환경을 올립니다. **jsdom 통과는 브라우저 증거가 아닙니다**(`E2E-1`) — 레이아웃·실제 스크롤·터치는 여기서 보이지 않습니다.
* 베이스라인 도구의 단위 테스트는 `node:test` 러너(`pnpm test:baseline`)가 돌리며 루트 `pnpm test`에 포함됩니다. **실제 스택 기동·덤프는 단위 테스트의 대상이 아닙니다.**
* SQL 회귀는 `supabase/tests/`에 있고 실행기는 `pnpm test:db`(격리 로컬 스택)이며, CI의 `DB 회귀` 잡이 같은 러너를 부릅니다. 절차와 전제는 [OPERATIONS.md](./OPERATIONS.md) §4.3.

---

## 4. 현재 확인된 차단 요소 — 요약

우선순위와 실행 순서는 [MASTER_PLAN.md](./MASTER_PLAN.md)가, 보안 판단 근거는 [SECURITY_REVIEW.md](./SECURITY_REVIEW.md)가 소유합니다. 여기서는 **사실과 참조 ID만** 둡니다(우선순위를 다시 매기지 않습니다).

| 사실 | 근거 | 후속 |
| :--- | :--- | :--- |
| 알림 채널 어댑터가 없어 초대 안내·비밀번호 재설정 링크·결재 알림이 **실제로 나가지 않습니다.** 다만 그 사실이 이제 `ok: false`로 보고되고(공급자 키가 있어도), 재설정 안내 집계도 `notified`만 발송으로 셉니다 — **거짓 성공 보고는 해소, 실발송은 미구현** | `supabase/functions/_shared/notifications.ts`, `apps/works/src/features/program/participantAccessHooks.ts` | `SEC-2`(잔여), 보안 `F-2`(부분 해소) |
| `ALLOWED_ORIGINS`가 **설정되지 않은 경우** 코드가 모든 origin을 허용합니다(유예 모드). 운영 환경에 이 값이 실제로 설정되어 있는지는 저장소에서 확인할 수 없습니다 | `supabase/functions/_shared/cors.ts` | `SEC-3`, 보안 `F-3` |
| 배포가 같은 SHA의 CI 성공을 전제로만 진행되도록 게이트가 들어갔으나, **그 게이트가 GitHub에서 실제로 동작한 이력은 확인하지 않았습니다** | `.github/workflows/deploy.yml` | `OPS-1`(잔여: 실행 이력), 보안 `F-5`(부분 해소) |
| Edge Function·마이그레이션의 배포 경로가 워크플로에 없습니다 | `.github/workflows/deploy.yml` | `OPS-2` |
| 신규 DB의 테이블 권한이 정책과 어긋나 있던 범위는 **마이그레이션 4건으로 정리**되었고 회귀가 고정합니다(§3.1). **남은 것은 전역 감사입니다** — 역할×워크스페이스 전수 확인, 오래되어 쓰이지 않는 ACL 정리, `open_program_guest_access`의 지원 범위(FUND·M&A)와 `guest_invitations` 정책 범위(PROJECT·GUEST)의 **불일치**가 그것입니다. 불일치는 **관측 사실**이며 지금 RLS를 넓히는 근거로 쓰지 않습니다 | §3.1, [15_authorization_inventory.md](./docs_dev/15_authorization_inventory.md) | `AUTHZ-1`·`AUTHZ-2`(전역 감사 잔여), 보안 `F-7` |
| `guest-temp-resolve`가 [TempGuestPage.tsx](../apps/guest/src/pages/TempGuestPage.tsx)에서 호출되는데 **저장소에 소스가 없습니다**(`/g/:token`). 배포본이 따로 있다면 그 인가는 재생·검토가 불가능하고, 없다면 임시 게스트 화면이 조용히 깨져 있습니다 — **어느 쪽인지 저장소로는 확정할 수 없습니다** | `apps/guest/src/pages/TempGuestPage.tsx` | `AUTHZ-1` 부채(다시 꺼낼 조건은 MASTER_PLAN) |
| Edge Function 다섯 곳(`aiFill/run`·`aiFill/extractRun`·`material-download`·`employee-create`·`link-metadata`)이 `is_active`를 보지 않거나 `public.users`를 조회하지 않습니다 — **관측된 것은 검사의 불균일이며, 실제 영향은 경로마다 따로 확인해야 합니다.** 뒤이은 호출자-토큰 질의·권한 검사가 막아 주는 경로가 있으나 **다섯 곳 전부에 대해 증명되지 않았으므로 일괄로 안전하다고 적지 않습니다** | `supabase/functions/` 해당 파일 | `AUTHZ-1` 부채(다시 꺼낼 조건은 MASTER_PLAN) |
| SQL 회귀는 현재 트리에서 전수 통과하지만(§3) 그것은 **격리된 신규 DB 기준**입니다 — 최신 마이그레이션의 **운영 반영 여부는 저장소에서 확인할 수 없습니다** | §3 | `DB-1`(운영 반영 확인) |
| 베이스라인 산출물은 **작업 트리에 확보**되었으나(§3), 그것이 보증하는 것은 **cutoff 시점 스키마의 재현성 하나**입니다 — 신규 환경 부트스트랩·복구 경로는 여전히 **실행해 본 적이 없고**, 커밋·배포도 하지 않았습니다 | `supabase/baseline/`, [12_database_baseline_operations.md](./docs_dev/12_database_baseline_operations.md) §5·§6 | `DB-1`(전환 절차는 별건) |
| GUEST 러너·게스트 인증 경계 테스트·DB 회귀 러너·함수 정적 검사가 자동 실행 경로에 들어왔고, 2026-09-12에 **`richText` 정화기 회귀**가 실제 DOM 기준으로 추가되었습니다(§3). 남은 공백은 **Deno 런타임·PostgREST/RLS·브라우저 경로**입니다 | `.github/workflows/ci.yml`, `apps/guest/src/lib/richText.test.ts` | `TEST-3`(완료), `E2E-1`, 보안 `F-6`(부분 해소) |
| 무인증·저인증 입구(익명 제출, 공개 파일, 게스트 로그인)에 출처 단위 호출 빈도 제한이 없습니다 | `supabase/functions/application-submit`, `public-module-file`, `guest-auth-login` | `SEC-4`, 보안 `F-4` |
| 브라우저 E2E 기준, 오류·실패 수집 및 경보 체계, 운영 환경 구성은 **저장소에서 확인되지 않습니다.** 저장소 밖에 존재할 가능성은 배제하지 않습니다 | — | `E2E-1`, `OBS-1` |
| 워크스페이스 예산·지출은 **저장소 구현·Codex 로컬 검증 완료**입니다. 전체 lint/typecheck/test/build와 격리 DB pgTAP **21파일/580건**(예산 52건), 공통 컴포넌트의 모의 응답 기반 360/768/1280px 브라우저 검증을 통과했습니다. **운영 DB 적용·실제 계정 결재 E2E·배포는 미실행**이며 기존 DB 베이스라인 불일치는 별도로 남아 있습니다 | [workspace_budget_execution.md](./docs_planning/workspace_budget_execution.md) 최종 검증, `supabase/migrations/20260913190000_*`·`20260913200000_*` | 운영 적용 전 베이스라인 정리·실제 계정 검증, `E2E-1` |
| 폐지된 HUB 명칭이 `apps/works/src/features/hub/` 폴더명으로 남아 있고(내용은 OFFICE 게시판·대시보드), 일부 파일이 500줄 가이드를 넘습니다 | `apps/works/src/features/hub/`, `REF-1`의 목록 | `REF-1` |

### GUEST 로그인 정책 (사용자 확정)

GUEST 로그인은 이메일 아이디 + 비밀번호이고, 비밀번호 미설정 계정은 **고정 초기 비밀번호**로 들어와 세션 대신 설정 티켓만 받고 본인이 개인 비밀번호를 정한 뒤에야 세션이 열립니다(`guest-auth-login/handler.ts`). 설정 이후에는 개시값이 통하지 않으며 기존 계정 잠금은 그대로 걸립니다. 이 때문에 알림 미발송이 로그인 경로 자체를 막지는 않습니다.

**이는 2026-09-12 사용자가 확정한 운영 정책이며, 개시값은 2026-09-14 사용자 지시로 원장 연락처에서 고정 문자열로 바뀌었습니다.** 그에 따라 **연락처는 자격증명이 아닌 선택 칸**이 되었고 계정 등록의 필수 칸은 **이름·이메일·소속**입니다. 개시 구간 이메일의 취급과 배포는 저장소 밖의 **오프라인 운영 정책**이 담당하며, 사용자가 그 기밀 취급을 확정했습니다. 정본은 [3_9_1 §6](./docs_planning/3_9_1_guest_unified_account.md)이고, 그 정책 안에서의 동작 확인은 [MASTER_PLAN.md](./MASTER_PLAN.md) `AUTH-1`, 경계 단언은 `TEST-2`가 맡습니다.

**골격은 그대로이고 개시값만 바뀌었습니다.** 2026-09-12에 들어간 것은 ① 판정 본체를 배선(`Deno.serve`)에서 분리한 **동작 보존 정리**와 ② 규칙을 고정하는 단위 테스트입니다(덮은 경계 목록은 [MASTER_PLAN.md](./MASTER_PLAN.md) `TEST-2`). 2026-09-14에는 그 테스트를 고정값 기준으로 옮겨 적고 연락처 없는 계정의 로그인을 함께 덮었습니다. **이 테스트는 Node에서 DB 클라이언트를 대역으로 두고 돌므로, 배포된 인증이나 RLS가 동작한다는 증거가 아닙니다.** **잔여 위험은 커졌습니다**: 개시값이 전 계정 공통이라 비밀번호 설정 전 계정의 **이메일만** 알려져도 제3자가 먼저 설정 티켓을 받는 사칭이 성립하며, 그 경로에는 실패가 나지 않아 계정 단위 잠금도 걸리지 않습니다([SECURITY_REVIEW.md](./SECURITY_REVIEW.md) `F-0`, 시도 상한은 `F-4`). **사용자가 알고 내린 결정입니다.** 오프라인 집행 여부는 저장소에서 확인할 수 없습니다. 재설정 링크는 현재 대체 경로가 없습니다.

> [!NOTE]
> 종전 판의 "초대 토큰 전환·초기 로그인 경로 차단" 권고는 **확정 정책으로 승계되어 효력을 잃은 제안**이며, 고쳐진 취약점이 아닙니다.

#### 통합 GUEST 계정 관리 (2026-09-13, 저장소 구현 완료)

> [!NOTE]
> 이 절은 **2026-09-13 시점의 기록**입니다. 필수 칸·개시값·명부 추가 창의 구조는 2026-09-14에 바뀌었고, 그 내용은 아래 §3.7이 가집니다.

* **사용자가 ADMIN 초기화를 확정**했고, 종전의 "담당자 초기화를 두지 않는다"는 판단을 그 결정이 대체합니다. 정본은 [3_9_1 §6.2](./docs_planning/3_9_1_guest_unified_account.md)입니다(개시값은 2026-09-14에 고정값으로 바뀌었습니다 — §3.7).
* `/guest-accounts` 한 화면을 사업부·M&A팀·투자실 사이드바 하단에서 엽니다. 일반 내부 사용자는 수기 계정 생성과 마스킹 조회를 하며, 목록의 `대용량 업로드` 버튼은 `/guest-accounts/bulk` 전용 페이지로 이동합니다. ADMIN은 같은 목록에서 원본 조회·체크박스 일괄 정지/해제·영구 삭제와 상세 연락처 수정·비밀번호 초기화를 합니다.
* 계정 목록은 `스타트업 / 전문가 / BUYER / SELLER / FUND / 미연결` 여섯 탭이며 `전체`는 없습니다. 복수 자격 계정은 해당 탭마다 보이고, 미연결은 원장 인격과 FUND 참여가 모두 없는 계정을 받습니다. 분류 Y/N 열은 제거했고 서버가 탭 필터 후 전체 건수와 페이지를 계산합니다.
* 수기 생성은 이름·이메일·연락처 한 줄을 늘리는 방식이고, 대용량 전용 페이지는 CSV·XLSX 10,000줄, 화면 100줄 페이지, 서버 200줄 묶음을 지원합니다. 모든 GUEST 계정은 전사 공통이고 NETWORKS 인격 연결은 선택입니다. 행별 `네트워크 원장에서 찾기` 모달은 NETWORKS 구분 탭·검색·서버 페이지네이션·공용 DataTable로 구성되며, 체크한 여러 NETWORKS 행을 한 번에 계정 입력 줄로 넣습니다. 정상 줄은 생성하고 중복·오류 줄만 사유와 함께 남기며 응답이 끊긴 줄은 `확인 필요`로 분리합니다.
* ADMIN 경계는 **RPC 셋**(`admin_update_guest_contact`·`admin_reset_guest_password`·`guest_password_commit`)과 **단명 티켓의 세션 판 묶기**(`supabase/functions/_shared/guestAccount.ts`의 `sv` 클레임, `guest-auth-password`·`guest-auth-context`가 회수 시점에 대조)가 담당합니다. 초기화·연락처 수정은 계정만 고치고 원장은 건드리지 않으며, `is_active`를 바꾸지 않고 변경 전/후를 `audit_logs`에 남깁니다. 연락처가 실제로 바뀌면 **살아 있는 재설정 링크도 함께 비웁니다**(비밀번호·잠금은 유지).
* **비밀번호 쓰기는 조건부 한 경로로 모았습니다**(`guest_password_commit`, `service_role` 전용 INVOKER). 기대 판·기대 해시가 모두 맞을 때만 저장하고, 티켓 경로는 성공 시 판을 올려 티켓을 소진하며 착지는 돌려받은 판을 씁니다. 종전에는 해시를 만드는 동안(수백 ms) 들어온 초기화를 옛 티켓이 덮어쓸 수 있었습니다(3_9_1 §6.2.2).
* 재설정 링크 발급·소진은 토큰·만료·계정 상태·세션 판을 한 트랜잭션에서 비교 후 교체하며 같은 링크의 동시 사용은 한 건만 성공합니다. 보낼 이메일·전화가 모두 없으면 토큰도 발급하지 않습니다.
* 프로젝트/FUND 상세의 `GUEST 계정 추가`는 기존 계정 추가·명부 제외만 수행하고 계정을 만들지 않습니다. 제거해도 계정·원장·참가 명부·인격·다른 프로젝트/FUND 참여는 남습니다. 직접 INSERT·UPDATE로 숨은 M&A 계정을 배정하는 우회도 트리거가 차단합니다.
* 검증: §3의 WORKS·GUEST Vitest, 함수 타입체크, 전체 pgTAP이 통과했습니다. **운영 DB 반영·Edge Function 배포·브라우저 E2E는 하지 않았습니다**(`DB-1`, `E2E-1`).

---

## 5. 해소된 과거 차단 요소

종전 판에서 차단 요소로 적혀 있었으나 현재 소스에서 성립하지 않는 항목입니다. 되돌아오지 않았는지 확인할 때의 기준으로 남깁니다.

| 과거 서술 | 현재 사실 | 근거 |
| :--- | :--- | :--- |
| 알림 발송 일반 창구가 **토큰만 확인하고 임의 수신처·문안을 받음** | 창구가 닫혔습니다 — `POST`는 403 `endpoint_disabled`, 그 밖의 메서드는 405, CORS 프리플라이트만 남습니다. 요청 본문이 수신처·문안을 정하는 경로가 없고, 거절이 요청과 무관하며 **발송기·특권 클라이언트·바깥 요청 부작용이 없다는 것**까지 회귀가 고정합니다. 게스트 초대·비밀번호 재설정 경로는 그대로입니다 | `supabase/functions/notifications-dispatch/handler.ts`·`handler.test.ts` (`SEC-1`, 보안 `F-1`) |
| 베이스라인 산출물이 **없어** `db:baseline:check`·`verify`가 비교 대상을 갖지 못함 | 산출물 한 쌍이 작업 트리에 있고 두 명령이 실제 판정을 냅니다. 값·절차·한계는 [12_database_baseline_operations.md](./docs_dev/12_database_baseline_operations.md)가 소유합니다 | `supabase/baseline/manifest.json` (`DB-1`) |
| 알림 미발송 때문에 GUEST **OTP 로그인 경로가 차단**됨 | 로그인은 이메일+비밀번호 커스텀 JWT이며 OTP 발송에 의존하지 않습니다(초기 비밀번호의 위험은 §4 참고) | `supabase/functions/guest-auth-login/handler.ts`, `apps/guest/src/auth/guestAuthService.ts` |
| 게시판 일부가 **인메모리 데모 저장소**에 의존 | `public.board_posts`를 직접 읽고 쓰며 열람 범위는 RLS가 강제 | `apps/works/src/features/hub/boardPostsApi.ts` |
| 루트 린트가 `hiworks_backup/`까지 검사해 수천 건 보고 | ESLint·git 모두 해당 경로를 무시 | `eslint.config.mjs` ignores, `.gitignore` |

---

## 6. 갱신 규칙

* 운영 배포, 품질 게이트, 인증 방식, DB 반영 상태가 바뀌면 같은 작업에서 본 문서를 함께 갱신합니다.
* 확인하지 않은 상태는 `완료`로 추정하지 않고 `미확인`·`미검증`·`미실행`으로 적습니다. 저장소에서 보이지 않는 것은 `없음`이 아니라 `확인되지 않음`으로 적습니다.
* 검증 결과에는 실행 날짜·시각, 대상 브랜치·커밋, 검증 범위 밖의 변경이 있었는지를 남깁니다.
* 상세 이력은 [PROGRESS.md](./docs_master/PROGRESS.md)에 두고, 여기에는 현재 판단에 필요한 요약만 유지합니다.
