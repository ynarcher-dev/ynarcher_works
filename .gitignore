# 의존성
node_modules/
.pnpm-store/

# 빌드 산출물
dist/
build/
out/
.turbo/
*.tsbuildinfo

# 환경 변수 (Secret은 절대 커밋하지 않는다 — docs_dev/4_security_privacy_policy.md)
.env
.env.*
!.env.example

# 로그
*.log
npm-debug.log*
pnpm-debug.log*

# 에디터/OS
.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json
*.swp

# 테스트 커버리지
coverage/

# Supabase 로컬
supabase/.branches/
supabase/.temp/

# Claude Code 개인 설정(권한 등) — 커밋 금지
.claude/settings.local.json

# 하이웍스 원본 백업(우리 소스가 아니다). 커밋 대상이 아니고, 추적되지 않은 채 남아 있으면
# git status가 늘 시끄러워 `git add -A` 한 번에 통째로 딸려 들어갈 위험이 있다.
hiworks_backup/
