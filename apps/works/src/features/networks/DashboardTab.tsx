/**
 * NETWORKS 대시보드 — 전면 재설계를 위해 콘텐츠를 비워 둔 상태.
 * 기존 구성(네트워크 현황 타일·분야/권역 분포·평가랭킹·최근 등록 피드)에 쓰이던 컴포넌트와
 * 집계 훅(`@/features/networks/dashboard`, `dashboardHooks.ts`)은 그대로 남아 있으므로
 * 재설계 시 필요한 것만 다시 가져다 쓴다.
 */
export function DashboardTab() {
  return null
}
