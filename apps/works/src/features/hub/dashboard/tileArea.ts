/**
 * 대시보드 좌측 열 카드가 **미리** 잡아 두는 본문 높이(타일 세 장 자리).
 *
 * 담긴 것이 0장이든 3장이든 카드 높이가 같아야 좌측 열이 글이 쌓일 때마다 출렁이지 않는다.
 * 값의 근거: 타일 한 장 45px(위아래 여백 12+12 + 제목 줄 21) × 3 + 타일 사이 8px × 2 = 151px.
 *
 * 공지사항·체크리스트 두 칸이 나눠 쓴다 — 나란히 선 두 상자라 한쪽만 바뀌면 곧바로 어긋난다.
 */
export const DASHBOARD_TILE_AREA = 'min-h-[9.5rem]'
