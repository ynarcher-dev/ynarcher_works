/**
 * 대시보드 좌측 열 카드가 **미리** 잡아 두는 본문 높이(타일 세 장 자리).
 *
 * 담긴 것이 0장이든 3장이든 카드 높이가 같아야 좌측 열이 글이 쌓일 때마다 출렁이지 않는다.
 * 값의 근거: 타일 한 장 45px(위아래 여백 12+12 + 제목 줄 21) × 3 + 타일 사이 8px × 2 = 151px.
 *
 * 공지사항·체크리스트 두 칸이 나눠 쓴다 — 나란히 선 두 상자라 한쪽만 바뀌면 곧바로 어긋난다.
 * 그래서 두 칸은 자리뿐 아니라 **줄의 생김새와 넘칠 때의 처리도 같은 규칙**을 쓴다(2026-08-26):
 * 줄 여백은 둘 다 `p-3`(45px — 공지는 `py-2` 37px이었다), 세 줄을 넘는 만큼은 둘 다 자리 아래
 * '전체 보기' 한 줄로 넘긴다.
 */
export const DASHBOARD_TILE_AREA = 'min-h-[9.5rem]'

/**
 * 자리 아래 '전체 보기' 한 줄 — 두 카드가 같은 규격을 나눠 쓴다.
 *
 * 건수와 무관하게 **늘 세운다.** 넘칠 때만 세우면 한쪽 카드에만 줄이 하나 더 생겨 40px이
 * 어긋나고, 나란한 짝은 stretch로 따라 늘며 빈 자리만 커진다(2026-08-26에 체크리스트가 겪던
 * 일이다). 자리 안이 아니라 아래인 것도 같은 이유 — 안에 넣으면 타일 한 장을 밀어낸다.
 *
 * 버튼에 건수를 적지 않는다: 머리의 `[N]`이 이미 세어 두었고, 두 카드의 셈 기준이 달라
 * (공지는 전체 · 체크리스트는 미완료) 버튼에서 또 세면 한 카드 안에 숫자 둘이 어긋나 선다.
 */
export const DASHBOARD_CARD_FOOTER = 'mt-2 w-full justify-center text-gray-500'
