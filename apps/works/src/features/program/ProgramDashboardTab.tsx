/**
 * 사업 대시보드(AC/M&A/PROJECT 공용) — 전면 재설계를 위해 콘텐츠를 비워 둔 상태.
 * 기존 상태 요약 타일은 제거했고, 타일 컴포넌트(`@/features/networks/dashboard/StatusTileGrid`)와
 * 사업 목록 훅(`@/features/program/hooks`)은 그대로 남아 있다.
 */
export function ProgramDashboardTab() {
  return null
}
