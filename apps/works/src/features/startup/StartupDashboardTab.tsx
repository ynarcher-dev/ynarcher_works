/**
 * STARTUP 대시보드 — 전면 재설계를 위해 콘텐츠를 비워 둔 상태.
 * 기존 구성(일반현황 타일·분포 카드·최근 등록 피드)에 쓰이던 컴포넌트와 집계 훅은
 * `@/features/startup/dashboard`, `@/features/networks/dashboard`에 그대로 남아 있으므로
 * 재설계 시 필요한 것만 다시 가져다 쓴다.
 */
export function StartupDashboardTab() {
  return null
}
