/**
 * @ynarcher/ui — 순수 UI 레이어(Pure UI Layer) 진입점입니다.
 *
 * 디자인 토큰 기반 Atom 컴포넌트만 이 패키지에서 export 합니다.
 * 데이터 결합(@supabase/supabase-js, @tanstack/react-query 등)은 금지합니다.
 * 실제 컴포넌트 구현은 PROGRESS.md Phase 4에서 채웁니다.
 */
export const UI_PACKAGE_NAME = '@ynarcher/ui'
