{
  "name": "@ynarcher/ui",
  "version": "0.0.0",
  "private": true,
  "description": "순수 UI 레이어 — 디자인 토큰 기반 Atom 컴포넌트 (데이터 결합 금지)",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "echo \"[ui] 소스 export 패키지(빌드 불요) — 앱이 TS 소스를 직접 컴파일합니다.\"",
    "lint": "echo \"[ui] lint은 루트 eslint . 로 수행합니다.\"",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist .turbo"
  },
  "peerDependencies": {
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "dependencies": {
    "clsx": "^2.1.1",
    "tailwind-merge": "^2.6.0"
  },
  "devDependencies": {
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "react": "^19.0.0",
    "typescript": "^5.7.0"
  }
}
